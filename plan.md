# LMX plan

**Authoritative plan.** Adopted 2026-09-06 by independent review of the three
candidate roadmaps (#64, #65, #66) after integration PR #63 merged into `main`
(`032c5cc`). This document supersedes all previous plans; the prior roadmap
remains readable at [`d18fefc:plan.md`](https://github.com/uwplasma/LMX/blob/d18fefce2eb241b4c8d8bc733e914e530e8ce4d4/plan.md)
and the candidate drafts in their closed PRs. It is written so that an
independent agent can execute it phase by phase without further context.

Change control: this file changes only at phase boundaries or when a decision
in Section 4 is reversed by a new ADR in `docs/adr/`. Day-to-day notes live in
PRs and issues, not here.

---

## 0. How to use this document

| If you are… | Read | Then |
|---|---|---|
| starting a phase | Section 1 (targets), Section 4 (decisions), the phase in Section 6, Section 12 (agent instructions) | open the phase's first PR |
| reviewing a PR | the phase's exit gates in Section 6 and the validation rows in Section 7 it claims | require the numbers, not "implemented" |
| deciding whether to change the numerics | Section 3 (what the literature says) and the relevant ADR in Section 4 | write a new ADR before coding |
| looking for the current state | Section 2 | update Section 2 only at phase boundaries |

Terminology: *fully developed* = 2-D cross-section solves (Hartmann, Shercliff,
Hunt); *3-D core* = the extruded duct/pipe solver in spatially varying fields;
*Q2D* = the Sommeria–Moreau depth-averaged vorticity model; *B1/B2* = the ALEX
benchmarks of Smolentsev et al. (2015); *lane* = a code path that exists only to
serve one benchmark; *gate* = a measured condition with a stated tolerance.

---

## 1. Goals and measurable targets

| # | Goal (owner) | Target for release 2.0 |
|---|---|---|
| G1 | Physics of 3-D fields, Q2D and others, with engaging plots and movies | Seven examples, each producing a publication-quality figure; three movies (Q2D forced turbulence, 3-D fringing duct current loops, stellarator-coil-field channel); every figure regenerated by `scripts/make_showcase_figures.py` |
| G2 | Slim repository below 10 MB | Ordinary clone ≤ 5 MB (3.5 MB today); Git media ≤ 1 MB (WebP); movies and raw data as release assets |
| G3 | Minimise lines and files, maximise figures and performance | Package ≤ 9,000 source lines and ≤ 14 modules (17,954 / 26 on 2026-09-13, of which the new core is 2,833); tests ≤ 1.2× source lines; no lane that serves one benchmark |
| G4 | CPU and GPU, GPU much faster | **open, re-measured 2026-09-13 (D15)**: the 2026-09-07 float32 numbers (29.6× on the 3-D core at 128³, 54.5× on Q2D at 1024²) were taken with TensorFloat-32 on, which JAX enables by default on Ampere; the core's float32 accuracy was 3e-4, not 3e-7, and true float32 costs 15–30 % more per step. Float64 is 3.4× and 17.0×. The "1/64" explanation was wrong: a true-float32 contraction beats float64 by 0.9× at 64³, 4.1× at 128³ and 12× at 192³. Target: a float64-accurate core (mixed precision, D16) at 128³ ≥ 10× the 36-core CPU on one A4000, Q2D at 1024² ≥ 20×, crossover sizes reported, matmul precision recorded in every JSON |
| G5 | Parallelised with strong scaling | **not met, measured**: two A4000s run the Q2D solve to a bit-identical answer but at 0.25 efficiency in float64 and 0.14 in float32 at 2048², because the cards are PCIe-joined with no NVLink and the unaided partitioning of a 2-D transform all-gathers every step. Split from 2026-09-13: **G5a** ensembles across the two cards (Ha sweeps, design, gradient checks) at ≥ 0.9 efficiency; **G5b** spatial decomposition only where a run does not fit one card, or ≥ 0.6 measured at 256³ with an explicit slab (step 2.4b) |
| G6 | FreeMHD, OpenFOAM and other codes as inspiration | Ni et al. consistent-and-conservative scheme (FreeMHD, HIMAG, epotFoam, MHD-UCLA); CaNS / CaNS-EIGEN and Oceananigans for mixed transform–tridiagonal Poisson solves; Krasnov et al. 2023 for the thin wall inside a direct solver; jax-cfd solver architecture; jaxDecomp for distributed transforms |
| G7 | B1/B2 validation | **tiered (D20)**: ANL analytic fringe within 1 %; B2 axial pressure gradient within 5–10 % of experiment through the inertialess core-flow model with the measured field and the pressure-tap correction, transverse Δp by shape and sign; code-to-code (FreeMHD, core-flow model, 3-D core at moderate N) within 1–2 %; B1 through the core-flow model; a resolved 3-D B2 only as a cost-bounded cross-check |
| G8 | Retire experimental lanes | The B1/B2 and generic extruded lanes (6,002 lines) deleted from `main` after the new core carries the fully developed cases at parity (1.10b) and the fringing rows 6–7 (1.9); history kept at `lmx-b2-lane-v1` |
| G9 | Useful design results | A fully developed inverse-design example and a device-informed channel example with verified gradients |
| G10 | Thermal channel physics | Passive-scalar energy equation with conjugate walls on the new core, verified against laminar limits |
| G11 | Steady solves and adjoints at blanket Ha | Certified adjoints to Ha 1000 in the regression tier (today the tests stop at Ha 5 and the shipped tangent solve fails at Ha 300); a 3-D 128³ steady solve within 16 GB; Krylov iterations reported against Ha |

Non-goals before the release after 2.0: buoyancy, curved or mapped channels,
live coil or equilibrium derivatives, neutronics or structural coupling, free
surfaces, magnetic induction (Rm ≪ 1 stays), 3-D turbulent heat transfer.

---

## 2. State on 2026-09-13 (measured)

Numbers from an Apple M3 Max (Python 3.11, JAX 0.10.2, SOLVAX 0.20.0) and the
office RTX A4000 (GPU 0 and 1, both shared with other users' jobs), all at
`main` `270063d` with #123 open. The 2026-09-06 state is in the history of this
file; what it listed as broken is now either fixed (steady solver, `scan`
loop, explicit precision, VJP NaN) or superseded by the rows below.

### 2.1 What works

| Check | Result |
|---|---|
| Full local suite at `270063d` | 799 passed in 590 s (4 workers, shared host); the slowest 20 tests take 30–123 s each |
| Fully developed ducts on the new core | Shercliff/Hunt Ha 0–1000 within 0.4–2.3 % of `validation/shercliff.py`; second order; comparable to FreeMHD (0.31 % at Ha 1000) and Siriano et al. 2024 (0.4–0.6 %) |
| Pipe | fully developed only; 0.04–0.43 % against `validation/pipe.py`; conducting wall first order in the wall potential |
| Steady solve and adjoints | Newton–Krylov with IFT adjoints; failed roots and tangent solves certified (#121); objectives compile (#123: 3.8 s eager → 3.8 ms warm) |
| Mixed precision (D4), measured before building it | float32 fast-diagonal solve + one float64 residual correction: relative error 2.0e-11 at 128³ (A4000, 2.4 ms against 13.8 ms float64), 9.9e-11 on the Ha 1000 layer mesh (width ratio 2,831), 2.2e-11 through `solvax.iterative_refinement` as-is |
| Partial diagonalization, verified for correctness only | the assembled periodic and uniform-Neumann axis operators are diagonalized by the DFT and DCT-II to 1e-14; FFT + dense + batched `jax.lax.linalg.tridiagonal_solve` reproduces the dense solve to 1e-13 -- and is 0.84–0.89× of the dense BLAS route on CPU at ≤ 96³ and 0.01× on the A4000 at 64³–128³ (the batched cuSPARSE path costs 566 ms against 3.0 ms at 64³), so it is not built on this evidence (1.12) |
| Conducting-wall potential as a direct solve, verified | the thin-wall perturbation of the separable operator has rank 2·n_y (46 of 576 at 24²); a Woodbury correction of the fast solve reproduces the inner-GMRES route to 8e-12 |
| Steady operator, Stokes limit | symmetric negative definite in the face-volume inner product on uniform meshes (asymmetry 2e-15); preconditioned CG converges in 25 / 84 / 223 iterations at Ha 20 / 100 / 300 on 32² against 25 / 127 / 742 for GMRES(60)×60 with the same preconditioner |
| Q2D | IFRK4 pseudo-spectral; real transforms give 1.54–2.52× per stage with the dealiased nonlinear term identical to 5.7e-16 |

### 2.2 What does not

| Finding | Evidence | Consequence |
|---|---|---|
| Float32 GPU accuracy was measured under TensorFloat-32 | default float32 contraction differs from float64 by 3e-4 on the A4000; `jax_default_matmul_precision='float32'` gives 2.6–6.1e-7 and costs 15–30 % more per step (64³ 0.43 → 0.52 ms, 128³ 8.7 → 10.0 ms, 192³ 14.8 → 18.8 ms); refinement stalls at 4.6e-4 under TF32 and reaches 2e-11 without it; nothing in `src/` or `scripts/` sets the precision D4 requires | every float32 number in `benchmarks/results` is TF32; re-measure (2.0, 2.1); D15 |
| "An A4000 runs float64 at 1/64 of its float32 rate" was wrong | true-float32 contraction over float64: 0.9× (64³), 4.1× (128³), 12× (192³); the JSON already said 13× for the core and 4.2× for Q2D; the figure came from NVIDIA's TF32 TFLOPS rating (Section 3.2 item 8) | retracted in Section 1, the README and Section 3.2; the float64 core is memory- and size-bound, not "flop-bound at 1/64" |
| The solves are the step, and every axis is dense | 2 Poisson + 3 Helmholtz solves take 95–128 % of a CPU step (64³: 13.7 ms); 30 dense `O(N⁴)` contractions per step, including on the periodic uniform axis, which is Fourier-diagonal; the FFT + tridiagonal route is no faster than BLAS on CPU (0.84–0.89× at ≤ 96³) and 100× slower on the A4000 through the batched cuSPARSE `tridiagonal_solve`, while mixed precision gives 2.4–4.5× on the whole step at float64 accuracy | 1.11 first (mixed precision), 1.12 only on a measured gain with a Thomas sweep |
| The tangent and transpose solves are unpreconditioned | `steady._krylov` runs GMRES(60)×60 with no preconditioner while the primal uses the projection preconditioner and GMRES(400); at Ha 300 on 48² the primal converges and `value_and_grad` returns NaN, also with GMRES(400)×6; with the primal preconditioner the gradient is finite, matches central differences to 7.8e-9 and takes 21.5 s instead of 38 s; the adjoint tests stop at Ha 5 | 1.7c; the "adjoint to Ha 1000" wording of 1.7 was never measured |
| The steady solver cannot run in 3-D | a GMRES(400) basis over three components is 400 × 3N³ × 8 B ≈ 20 GB at 128³; the primal needs restart 600 at Ha 1000; on stretched meshes the steady operator is asymmetric by 3.7e-2 because the electromotive and force interpolations are not discrete adjoints (the same defect as the first-order ohmic identity), so CG does not yet apply there; with the projection preconditioner the iteration count grows about linearly with Ha (25 → 223 from Ha 20 to 300; > 400 at Ha 1000) | 1.7d, 1.7e; G11 |
| `duct_problem` at Ha 1000 does not converge | `lmx.duct_problem(hartmann=1000, cells=96)` with the Ha 1000 test's solver settings is rejected; the test passes on its own `_duct(64, 1000, ratio=1.5)` mesh | the public three-line duct is not qualified at Ha 1000; 1.7e reports the mesh envelope |
| The reporting loop of the fully developed path cannot certify (#113) | Hunt Ha 20, 24², c 0.05, from rest: update residual 1.4e-9 at 96 steps, 2.9e-9 at 400 steps, 1.4e-9 with a 10× larger time step; the field differs from the direct affine solve of the same linear problem by 1.4–5.1e-8; each step syncs to the host (`cases.py:868`) at 180–720 ms | D19: report through `affine_fixed_point_gmres`; #113 closes there |
| The new core is slower than the legacy solver it should replace | same Shercliff duct, warm: 2.84 s against 1.29 s at Ha 20 on 32²; 15.6 s against 1.16 s at Ha 100 on 48² | 1.10b waits for 1.7c–1.7e and 1.11; parity is its gate |
| The conducting-wall closure is first order and its operator asymmetric | wall potential taken as the adjacent cell value; ohmic identity first order through a conducting wall; the pipe needs 96 radial cells for 0.43 %; the assembled potential operator with a conducting wall is asymmetric by 0.27 relative; the potential is an inner GMRES at rtol 1e-12 inside the outer Newton–GMRES | 1.3b (D18) |
| Nothing since 2026-09-08 advanced the critical path | #106–#121 and #123: ≈ 600 production lines across 13 PRs with ≈ 100,000 characters of description (#123: 27,500 for +23/−10; #108: 24,000 for 41 lines); Q2D reporting trims of 3–10 %; no step of 1.9, 1.10b or Phase 4 moved; all seven examples and `design.py` still run the legacy lanes; ≈ 10,850 of 15,909 test lines exercise legacy paths | D21 |
| Documentation claims ahead of tests | README: "differentiate … wall conductance, geometry" holds only on the legacy extruded lane; adjoint numbers quoted from the Ha 5 test; `k^-3` range is a guide line, not a measured slope; the Q2D "3,000 steps in 20 s" is the figure script, not the demo; `docs/validation` and the Q2D tutorial quote a 25.78× speed-up from JAX 0.6.2 that the current JSON puts at 14.15×; "no convective transport" is stale for the new core | fixed in the same PR as the plan or listed in 7.2 |
| Benchmark provenance | `benchmarks/results/*.json` name commit `a0b2c2b-dirty`, which no longer exists after the #122 history rewrite; the office nightly of #75 is not deployed; GPU step times vary 2× between runs on the shared box (128³ float32: 4.0 ms recorded, 8.7 ms on 2026-09-13) | 2.0, 2.3 |

### 2.3 Pull-request state after this review

| PR / issue | Disposition |
|---|---|
| #123 | merged (`b5dbb06`) |
| #113 | closed by #129 (D19: certified affine reporting) |
| #75 | CI tiers merged; the office nightly and benchmark publication remain the open half |
| #122 | done; fresh clones only, no pre-rewrite ancestry |
| `codex/*` branches other than #123 | not development lanes; inspect their PR state before touching |
| #130 | open: the Li-AlN intact-wall model does not certify (residual 2.7); needs a thick-wall option, which 1.3b (#136) did not build |
| #145 | open: a steady solve does not certify a varying-field duct at Ha 100 (CG floor near 1e-9 with a 3-D pressure; `steady._finish` certifies the unweighted residual while CG converges in the weighted norm); gates 1.9b–1.9d |
| #146 | open: `lmx.pipe` pairs the thin-wall current with the tangential field, the defect #144 fixed in `lmx.core3d` |

### 2.4 Progress after the review (2026-09-13 to 2026-09-14, `main` at `646b8d5`)

Each row replaces the matching 2.2 finding; numbers are from the PR descriptions,
measured on the M3 Max unless an A4000 is named.

| Step | PR | Result |
|---|---|---|
| 2.0 precision provenance | #126 | `jax_default_matmul_precision` pinned to `'highest'` when unset (by `enable_x64`, a case dtype, `ChannelProblem`, `Q2DProblem`); benchmark JSON records precision, TF32 override, XLA flags, driver, CUDA and load, and refuses float32 GPU reports without a true-float32 precision; float32 contraction 4.2e-7 from float64; stale README and validation claims corrected. Renaming the old JSON provenance moves to 2.1 |
| 1.7c preconditioned adjoint | #127 | the primal projection preconditioner in the tangent solve and its exact `jax.linear_transpose` in the transpose; transpose GMRES at Ha 100 on 24²: 3600 iterations and NaN → 92; gradients against central differences 2.1e-11 / 6.7e-9 (Ha 100) and 6.4e-10 / 7.8e-9 (Ha 300); warm compiled `value_and_grad` 0.16 s (Ha 100) and 2.6–3.6 s (Ha 300); the defect began at Ha 100, not 300. Ha 1000 stays with 1.7e |
| 1.11 mixed precision | #128 | `precision="mixed"` on the three factorizations and `ChannelProblem`; float64 floor after two Poisson / one Helmholtz correction on CPU and A4000 (≤ 2.4e-11 on 64³–192³ layer meshes; 2.0e-11 polar at Ha 1000); gradient 4.8e-11; trajectories within 2.1e-11; A4000 step 1.9× / 3.4× / 4.3× at 64³ / 128³ / 192³ on a shared card (absolute times to be re-measured in 2.1); TF32 stalls the correction at 2.7e-5 |
| 3.0 Q2D real transforms | #125 | `rfftn`/`irfftn` on the half plane; fields, history, `evolve_q2d` and its gradient within 1e-15 of the complex code; IFRK4 step 1.5–2.9× and `solve_q2d` loop 1.35–2.3× on a loaded host |
| 3b.4 / D19 affine reporting | #129 | the cold-start floor of #113 was a round-off mean subtraction in `solvers._solve_potential` that made the coupled map non-affine at 2e-8; steady mode is now one certified affine GMRES solve (21 iterations); cold Hunt Ha 20: fixed-point residual 3.0e-13, momentum and charge residuals from the assembled operator 4.0e-13 / 2.6e-13, field against a SciPy sparse direct solve 2.8e-13; the transient loop keeps diagnostics on the device; `status` is `converged`/`not_converged` and `steps` counts GMRES iterations in steady mode |
| 1.7d part i, adjoint-consistent EM | #131 | `ops.face_average` and its transpose carry the velocity to the electromotive force and the Lorentz force back; steady-operator asymmetry on `duct_problem(100, 32)` 1.0e-2 → 3.3e-14 (Ha 300 on 48²: 4.2e-13); ohmic defect 2.4e-4 → 2.6e-15; Shercliff errors Ha 20/100/300/1000 1.29/0.83/1.03/0.53 % → 1.03/0.75/0.96/0.48 %; Hunt c 0.027/0.1 1.04/0.52 % → 0.84/0.72 %; the pipe keeps its interpolation until 1.3b because the adjoint pair there exposes the first-order wall (conducting-pipe order 2.51 → −0.53) |
| 1.7d part ii, O(1)-memory steady solve | #135 | the insulating Stokes-limit steady state is one preconditioned CG solve in face-volume-weighted variables, differentiated by a symmetric `custom_linear_solve`; the preconditioner damps every component at σ\|B\|²/ρ (κ 8.9e5 → 3.8e3 on `duct_problem(300, 40)`); advection stays on Newton–GMRES(60); warm 2-D solves 15–25× faster; 3-D 64³ 0.95 s and 2.42 GiB against 16.4 s and 5.44 GiB |
| 1.3b second-order thin wall | #136 | each conducting wall is a sheet node of the one-dimensional operators, so the potential solve stays a direct Kronecker-sum solve (symmetric to 9.9e-16, 23.8× faster than the Krylov route it replaces) with a rank-4 corner correction; ohmic identity to round-off; conducting pipe 3.0 % → 0.239 % at 48 radial cells and order 2.00 under joint radial/azimuthal/layer refinement (the radial-only gate had stopped measuring the wall); conducting walls now use CG |
| 1.7e Ha-robust preconditioning | #137 | an exact field-line solve (Shercliff's induction form, pentadiagonal per line) for the periodic-axis component across a uniform axis-aligned field; CG iterations on `duct_problem` 48 cells 33/148/403 → 4/15/44 at Ha 20/100/300, and 714 → 68 (42–45 ms warm against 419–467 ms) on 64 cells at Ha 1000; Hunt through CG 4–5× fewer; every mesh of 24–128 cells converges from rest at Ha 20–1000; semicoarsening multigrid (3–9× slower) and recycling (≤ 20 %) not adopted |
| 1.9a varying imposed field | #144 | `ImposedField` of cell-centred arrays (a constant field as arrays is bit for bit the uniform path); `fringe_field` builds the ANL x0 = 3 fringe and its Votyakov solenoidal pair from one flux function (discrete divergence 0 on uniform cells, 2.6e-16 on tanh); energy identity and Stokes symmetry to round-off with a varying field, with and without a thin wall; adjoint against central differences 3.1e-11 / 5.8e-9 at Ha 20; stationwise Shercliff profiles within 0.72 % at variation length 25, while the local pressure gradient differs from the uniform value by 4.6 % (a physical 3-D correction); found and fixed a thin-wall half-cell current exerting J×B with no electromotive force (asymmetry 1.1e-3 with conducting side walls); certification at Ha 100 open as #145 |
| 1.10b parity (measurement) | #141 | on `0a71a8f`: warm parity met at Shercliff Ha 20/100/300 (ratio 0.10–0.16) and Hunt Ha 20/100 (0.11–0.21), not at Ha 1000 (2.31); #137 closes the Ha 1000 iteration gap; re-measured on current `main` in #141; cold starts about 2× slower; the cached Ha 1000 test reference is 0.60 % high (converged 0.000972103) |
| CI on `main` | #138, #139, #140, #143 | the public-solve test asserts the relative residual certificate (CG stops at the tolerance); a renamed JAX docs anchor; the main-only coverage lane sized for instrumented runs (30 min, 1500 s, 600 s per test); the steady coverage shard, which peaks at 11.85 GB with four workers against 16 GB runners, runs on two |

Still open: #145 (certify varying-field steady solves; gates 1.9b–1.9d),
#146 (pipe wall-current force), 1.9b–1.9d (non-periodic axial direction and
flow-rate multiplier, core-flow model, fringing rows 6–7), 1.10b (parity
re-measured in #141, then routing), 2.1 (controlled GPU campaign, #142), 2.3
(nightly), #130 (thick wall) and the Ha 1000 test reference.

---

## 3. What the research says

Five surveys were run for this plan (numerics at high Ha; JAX/GPU solver
architecture; Q2D turbulence; open validation data; presentation and process).
[V] = verified by reading the source; [M] = from memory. Full citations in
Section 11.

### 3.1 Numerics for high-Ha inductionless MHD

1. **Consistent-and-conservative scheme (Ni et al. 2007) [V].** Current as a
   face flux `J_n,f = −σ_f (φ_N − φ_P)/d_PN + σ_f (u_f × B_f)·n_f`; the potential
   Poisson equation assembled as the divergence of that same flux; the Lorentz
   force reconstructed by `(J×B)_c = (1/Ω) Σ_f J_n,f s_f (r_f − r_c) × B_f`.
   Implemented by HIMAG, MHD-UCLA, epotFoam, Blishchik 2021, Siriano 2024,
   Eardley-Brunt 2024, FreeMHD 2025. In the core `−∇p + J×B = 0` to O(Ha^-2), so
   any O(Δ) inconsistency between the two parts of J is amplified by Ha²; only
   this scheme reproduces Shercliff/Hunt at Ha 1000 (Ni Fig. 9).
2. **Staggered (MAC) variables** (Ni Part III) [V abstract]: face velocities are
   what `J_n,f` needs; no Rhie–Chow, no clipping.
3. **Thin conducting-wall condition** (Walker 1981; `j·n = ∇_τ·(c ∇_τ φ_w)`,
   `c = σ_w t_w/(σ a)`) [V]: valid for `t_w ≪ a/√Ha`, c ≪ 1; holds for B1
   (`t_w/a = 0.056`, c 0.027) and B2 (0.14, 0.07); how MHD-UCLA reaches Ha 15,000
   on a 99² stretched mesh (Smolentsev 2015 Table I). Requires its own thickness
   validity check (#65).
4. **Layer resolution** [V]: ≥ 6–8 cells across `a/Ha`, ≥ 10 across `a/√Ha`,
   geometric ratio ≤ 1.15, axial refinement across the fringe; ~120 cells across
   the half-width for B2, ~150 for B1.
5. **Stiffness** [V]: the velocity part of the Lorentz force is a damping with
   rate `σB²/ρ`; explicit treatment needs `Δt ∝ Ha^-2` (UKAEA: 5e-3 s at Ha 20 →
   1e-6 s at Ha 1000). Treat it linearly implicitly as a correction to the
   conservative face force, `F^{n+1} ≈ F^n_cons − σ(B²I − BBᵀ)(u^{n+1} − u^n)`,
   which vanishes at steady state; Crank–Nicolson diffusion.
6. **Steady solves are linear at high N** [V Planas/Badia]: matrix-free GMRES on
   the monolithic (u, p, φ) system with a block preconditioner (Stokes block +
   potential Laplacian); Newton–Krylov when inertia matters; pseudo-transient
   continuation only as a globaliser. Least-squares stationarity is not a zero
   PDE residual (#65).
7. **Fringing-field physics and observables** [V ANL/FPP/TM-228; HyPerComp 2008]:
   3-D pressure drop from axial current loops closing through side layers;
   observable `ΔP/(σ U B0² L)` between taps. ANL analytic case (square duct,
   `x0 = 3`, c = 0.02): total 0.0932 vs fully developed 0.0754. Dimensional B2:
   2a = 0.0878 m, t_w = 6.25 mm, σ_w = 1.51e6 S/m, B_max = 1.1 T, U = 0.337 m/s,
   Re = 15,574. B1: a = 0.0541 m, t_w = 3.01 mm, σ_w = 1.39e6 S/m, B_max = 2.08 T,
   U = 0.07 m/s, Re = 3,986.
8. **Field inputs** [V Votyakov 2009]: the ALEX profile was measured; use div- and
   curl-free analytic fringes so `B_x` is consistent with `B_y(x)`; run B2 with
   `B_y` only and with the solenoidal pair.
9. **Cylindrical operators** [V Verzicco–Orlandi; Oud 2016; #65]: transverse
   vector diffusion has the cross terms `Δu_r − u_r/r² − 2∂_θu_θ/r²` and
   `Δu_θ − u_θ/r² + 2∂_θu_r/r²`; use the `r·u_r` flux form on a staggered mesh;
   test smooth Cartesian modes, transverse translation, rigid rotation and an
   axis-crossing manufactured field before trusting a pipe result.

### 3.2 JAX and GPU solver architecture

1. **jax-cfd** [V source]: pytree containers with static grid/offset/BC metadata;
   pad-then-slice boundary conditions; fast-diagonalisation pressure solves
   (host-side `eigh`, tensor apply, RHS mean subtraction, nullspace cutoff);
   `lax.scan` as the only loop.
2. **JAX-Fluids 2.0** [V]: one-axis decomposition for two devices, `ppermute`
   halos, `jax.checkpoint` per step inside `scan`, float64 on GPU; fp32 2.5–3×
   faster than fp64 on GA102.
3. **jaxDecomp** [V]: pure-JAX slab/pencil FFTs and halo exchange; sharded
   `jnp.fft` also exists. Evaluate before writing our own.
4. **Fast diagonalisation on non-uniform tensor grids** [M]: two matmul passes
   per non-periodic axis; ~2e9 flops at 128³; trivial in fp32, compute-bound in
   fp64 on an A4000 (0.3 TFLOPS). DCT for uniform Neumann axes [V].
5. **Batched tridiagonal solves** [V]: `lax.linalg.tridiagonal_solve` is
   differentiable but calls cuSPARSE per system; measure against a vectorised
   Thomas sweep or SOLVAX.
6. **Implicit differentiation** [V]: `custom_root`/`custom_linear_solve`,
   optimistix `ImplicitAdjoint`, diffrax `RecursiveCheckpointAdjoint`; SOLVAX
   `root_solve`, `newton_krylov`, `checkpointed_fori_loop`. SOLVAX `root_solve`'s
   default tangent builds a dense Jacobian: supply matrix-free tangent/transpose
   solves (#65).
7. **Exponax/APEBench** [V]: ETDRK integrates the linear operator exactly; a
   linear drag is the Hartmann friction of the Q2D model; Kassam–Trefethen
   contour coefficients avoid cancellation.
8. **GPU practice** [V]: no per-step host sync; whole loop under `jit` + `scan`;
   `donate_argnums`; `block_until_ready` timing; persistent cache;
   `jax.profiler` for peak memory. RTX A4000: 448 GB/s, 16 GB, PCIe 4.0. The
   datasheet's 19.2 TFLOPS is a TensorFloat-32 tensor-core rating and its
   "fp64 = 1/64" is relative to that; measured true-float32 dense contractions
   beat float64 by 0.9× at 64³, 4.1× at 128³ and 12× at 192³ (Section 2.2).
9. **Expected performance** [M from bandwidth and JAX-Fluids]: fp64 one A4000
   ≈ 20–80 ms/step at 128³, 0.2–0.7 s/step at 256³; fp32 2–3× faster; 10–40× over
   a laptop CPU at ≥ 128³ but 2–5× at 64³; two-GPU efficiency 60–85 % at 256³,
   none at 64³. √-checkpointing 1,000 steps at 256³ fp64 exceeds 16 GB.
10. **Design throughput** (#65): independent batching (`vmap` over designs) before
    spatial sharding; logical CPU devices test partition parity, not scaling.

### 3.3 Q2D turbulence

1. **Model** [V Pothérat–Sommeria–Moreau 2000]: `∂_t u + u·∇u = −∇p/ρ + ν∇²u −
   (n/t_H) u + f`, `t_H = h²/(ν Ha_h)`, `n` = number of no-slip Hartmann walls
   (2 for a duct). Validity: Ha ≫ 1, N ≫ 1, laminar Hartmann layer (Re/Ha ≲ 380),
   scales `l_⊥ ≳ h N^-1/3`.
2. **Blanket regime** [V Smolentsev–Moreau 2006]: PbLi at h 0.1 m, B 5 T,
   U 0.1 m/s: Ha ≈ 1.2e4, Re ≈ 7e4, N ≈ 2e3, Re/Ha ≈ 6, t_H ≈ 5.6 s vs turnover
   1 s: friction-dominated Q2D is the blanket regime.
3. **Cases with literature numbers**: frozen decay (exact rescaling identity as
   ν → 0), electrically driven vortex lattice (Sommeria 1986: Kolmogorov constant
   3–7, condensation), Kolmogorov flow with drag (Kochkov 2021 reference fields),
   Tithof 2017 Kolmogorov-like flow with Rayleigh friction (Re_c = 7.60, q_c = 0.465
   for β = 1), MATUR-like shear layer, Q2D cylinder wake by penalisation (Hussam
   2011 tables).
4. **Numerics** [V Kassam–Trefethen]: ETDRK4, friction inside the exponential,
   2/3 dealiasing; cross-check against GeophysicalFlows.jl and Dedalus.
5. **Chaotic sensitivities** (#65, Blonigan 2014): AD through a short chaotic
   trajectory is not the sensitivity of a long-time average; optimise steady or
   checked finite-horizon objectives only.

### 3.4 Validation data and comparators

1. **ALEX B1/B2** [V]: digitised curves "available upon request from the first
   author"; ANL report open (OSTI 6789158). Digitise with WebPlotDigitizer in two
   independent passes, record calibration and repeat error, and request the
   originals in parallel.
2. **FreeMHD** [V]: MIT; validated on Shercliff/Hunt (Ha ≤ 1000, 0.3–3.9 %) and
   KIT's fringing pipe (Ha 2000, N 200), not on ALEX; inputs on Zenodo 13964055.
3. **Other comparators** [V]: GridapMHD (monolithic FEM, Hunt example),
   epotFoam/mhdFoam in the same OpenFOAM image, UKAEA input files
   (DOI 10.14468/43ys-0z67), Dedalus and GeophysicalFlows.jl for Q2D.
4. **Thermal references** (#65) [V]: Lienhard §7.2 laminar limits (Nu = 48/11
   uniform flux, 3.66 uniform temperature); Mistrangelo et al. 2025 heated
   conducting duct (Ha 235); Gardner–Lykoudis 1971 and Sukoriansky 1989 show that
   field effects on heat transfer depend on arrangement; Pirozzoli 2023 low-Pr DNS.

### 3.6 What the 2026-09-13 review adds

Five further passes (fast solvers; JAX and devices; high-Ha numerics, walls
and validation data; a citation check; a source-level read of other codes).
Tags as above; [C] = checked by the citation pass.

1. **Mixed transform–tridiagonal direct solves** [V source]: CaNS applies FFT,
   DCT or DST per axis by boundary condition on the staggered grid (cell-centred
   NN → DCT-II/III, DD → DST-II/III, mixed → type IV; Schumann & Sweet 1988),
   with the discrete symbol `−4 sin²(θ/2)/Δ²` and a Thomas sweep on the one
   stretched axis; the GPU version runs one thread per column. CaNS-EIGEN
   (Costa et al. 2026, arXiv:2603.09528 [C]) symmetrises stretched axes with
   `M^½ T M^−½`, keeps the eigenbases as one GEMM per axis and mixes FFT and
   GEMM per axis; on one GPU the GEMM route costs 2.8× the FFT route for the
   Poisson solve and 1.8× for the step. Oceananigans' Fourier–tridiagonal solver
   allows exactly one stretched axis and batches the tridiagonal sweep over the
   Fourier modes; its CG solver for immersed boundaries is preconditioned by the
   FFT solve of the underlying regular grid. `jax.scipy.fft` has DCT type II
   only [C]; DST-II/IV and DCT-IV come from `rfft` with Makhoul twiddles.
   `jax.lax.linalg.tridiagonal_solve` batches over leading axes, is
   differentiable in its diagonals and calls cuSPARSE `gtsv2` on GPU [C].
2. **The thin wall inside a direct solver** [V slides; C]: Krasnov, Akhtari,
   Zikanov & Schumacher, JCP 474:111784 (2023) combine the Walker condition
   with the Poisson row of the wall cell and either modify the corner of the
   per-mode tridiagonal system (wall normal to the Thomas axis) or scale the
   tangential rows by `h₁/(h₁ − 2c_w)` and eigendecompose the modified
   one-dimensional matrix; the factor changes sign when `2c_w > h₁`, so the
   conditioning has to be checked before adopting the second form. Their
   `tanh` meshes reach aspect ratio 400 with nine points in the Hartmann layer
   at Ha 1200; everything is direct, nothing iterates. Buzbee, Dorr, George &
   Golub (1971) and Proskurowski & Widlund (1976) are the capacitance-matrix
   (Woodbury) form of the same idea [C].
3. **Mixed precision** [C]: Carson & Higham (SISC 2017, 2018) and Buttari et
   al. (2007, 2008) for refinement; GMRES-IR is 19–31 % faster than double
   GMRES at restart ≤ 200 and needs about three times the iterations at
   restart 400 on one GPU Laplacian (Loe et al. 2021). Defect correction of a
   direct solve, which is what D16 does, has no restart. No published paper
   does fp32 fast diagonalization with fp64 refinement for MHD; the SC'25
   Pareto framework (arXiv:2508.10202) is the template for reporting it.
4. **Precision on Ampere** [C]: JAX's `DEFAULT` matmul precision "uses
   tensorfloat32 if available"; `'highest'` (or `'float32'`) gives true
   float32; float64 is unaffected.
5. **High-Ha preconditioning is open** [C]: Ma, Hu, Hu & Xu (2016) and
   Laakmann, Farrell & Mitchell (2022) are resistive MHD with no Hartmann
   claim; Li, Ni & Zheng (2019) is inductionless and its Ha-robustness could
   not be confirmed. GridapMHD's block preconditioners use scaled mass
   matrices for the pressure and potential Schur complements with grad-div
   augmentation, and no iteration counts against Ha are published. The
   textbook answer to grid-aligned anisotropy is line relaxation in the thin
   direction with semicoarsening in the other (Trottenberg, Oosterlee &
   Schüller ch. 5). Pseudo-transient continuation with SER and the index-1
   DAE extension (Kelley & Keyes 1998; Coffey, Kelley & Keyes 2003) is the
   globaliser for a projection-constrained steady solve.
6. **SOLVAX 0.20.0 already ships** `iterative_refinement`, `as_low_precision`,
   `tridiagonal_solve`, `cyclic_tridiagonal_solve`, `solve_fourier_helmholtz`,
   `low_rank_corrected`, `gcrot`, `recycled_linear_solve`,
   `schur_complement_precond`, `semicoarsening_hierarchy`, `line_smoother`,
   `pseudo_transient_continuation` and `shard_batch` [V signatures]; LMX uses
   none of them. The work in Phases 1.11–1.12 and 1.7c–1.7e is adoption on
   LMX's operators, not new kernels.
7. **Walls in other codes** [V]: Ni et al. 2007, FreeMHD and FreeMHD2 mesh the
   wall as a solid region (FreeMHD has no thin-wall patch; Hunt walls are
   `c = 10` solids); Siriano et al. 2024 use seven cells across the wall and
   three outer iterations; Blishchik et al. 2021 a harmonic-mean face
   conductivity. No published corner rule for a discrete thin-wall operator
   exists; LMX states its own (charge conservation across the corner cell, as
   Hua et al. 1988 split the corner between the two sheets). Layer resolution
   used in practice: ≥ 4 Hartmann cells with insulating walls (Siriano; 6
   adopted), ≤ 2 suffice with conducting Hartmann walls, 5–10 per layer
   (Blishchik), 7 and 15 (UCLA), ≥ 10 (FreeMHD).
8. **Inertialess core-flow model** [V Bühler lecture, ANL reports]: in the
   core `∇p = j × B`, so `p` depends on the two coordinates normal to `B`,
   `j_∥` is linear and `φ` quadratic along field lines, and integrating mass
   conservation across the duct gives one elliptic 2-D equation
   `∇·[(h/B²)∇p] = F(φ_t, p, φ_b)` coupled to the thin-wall condition on each
   Hartmann wall: three coupled 2-D PDEs on the wall surface, the 3-D field
   reconstructed analytically. Hua et al. 1988 matched the ALEX axial
   pressure gradient with it at N 540 as well as at 1.26e5; Ni et al. 2007
   overlay it with their 3-D solution at Ha 1000–2000. Development lengths at
   Ha 1000 in a pipe: 9.5 radii upstream, 7.5 downstream. Bühler's CoreFlow is
   KIT-internal; no open implementation exists.
9. **B1/B2 as targets** [V ANL/OSTI reports]: no ALEX report tabulates
   pressure, potential, velocity or `B(x)`; the curves are figures, digitised
   on request from Smolentsev; Hua et al. 1988 is an inertialess SOR solution
   stopped at 0.1 % nodal change on an unreported grid; ALEX square-duct walls
   are 0.66 cm on 8.8 cm (`t_w/L ≈ 0.15`), the transverse tap runs through the
   side wall, and Reed & Picologlou (1989) report side-wall instabilities at
   `Re_cr` 2650–5100 for `2700 < M < 5400`, below B2's Re ≈ 15,600. Inertia
   is a few to about ten percent of the 3-D excess pressure drop by the
   Mistrangelo et al. 2021 fit `Δp∞ + αHa^−1/2 + βN^−1/3`. COMSOL and HIMAG
   match the B2 axial gradient and are "somewhat off" on the transverse Δp.
   A resolved 3-D B2 needs ≈ 160²×400 cells (≈ 50 M unknowns; a GMRES(50)
   basis alone is 21 GB). Only NaK properties (ANL/FPP/TM-237) are tabulated.
10. **Other data** [V/A]: Smolentsev 2015 Table I (Shercliff and Hunt `c` 0.01
    flow rates at Ha 500–15,000); Tao & Ni 2013/2015 thick and asymmetric
    walls; KIT MEKKA pipe entry (Bühler, Brinkmann & Mistrangelo 2020, Ha to
    5485, paywalled, FreeMHD's fringing validation); FZKA 7245 sudden expansion
    (Ha 1000–5800, c 0.028); Siriano et al. NF 2023 WCLL 3-D pressure drops
    tabulated with mesh sensitivity; Jiang & Smolentsev 2024 insulating fringing
    correlation (80 cases, R² 0.9967); ASME V&V 20 for the uncertainty
    statement (`u_val = √(u_num² + u_input² + u_D²)`).
11. **Q2D** [V]: PSM2000 adds `(n/(Ha N))[(7/36) D_ū ū + (1/8) ∂_t(ū·∇ū)]` with
    error `max(1/(Ha N), N^−2, Ha^−2)`; SM82 alone holds only for weak inertia
    (transient growth matches 3-D for `Re/Ha ≲ 33`, Cassells et al. 2019) and
    never for B1/B2. Kassam–Trefethen coefficients by the 32-point half-circle
    mean of radius 1.
12. **Distribution** [V source]: jaxDecomp is a `custom_partitioning` primitive
    with `all_to_all` transposes and `ppermute` halos (FFT only, no DCT);
    JAX-Fluids uses `pmap` with `ppermute` halos; jax-cfd forces the dense
    route for any non-periodic axis, the same weakness LMX has today.
13. **Field position** [C]: no other GPU or JAX inductionless duct code was
    found. FreeMHD2 (2026) and Vertex-CFD (2025, Kokkos, full induction) and
    NekRS MHD (full induction) are the nearest; JAX-AMG (2026) wraps AmgX as a
    differentiable primitive and is a possible comparator.
14. **Corrections to the earlier surveys** [C]: arXiv 2607.10983, 2606.09001
    and 2605.13378 are JAX geometric multigrid for particle-mesh, JAX-AMG and
    AD-based JFNK, not fast-Poisson papers; arXiv 2403.03899 is Akhtari et al.
    2024 (magnetoconvection), not the Krasnov solver paper.

### 3.5 Presentation and process

1. **README** [V, 13 READMEs]: tagline → badges → install → ≤ 15-line example or
   gallery → docs link in the first screen; 300–1000 words; one hero ≤ 500 KB;
   caveats in docs.
2. **Process** [V Google small-CLs; DESC]: PRs are already small; the cost is
   per-PR ceremony. DESC: `unit`/`regression` markers, regression tests on PRs to
   the default branch only, benchmarks by label, weekly cron. GitHub: never a
   self-hosted GPU runner on a public repo's PRs; use a cron on the box.
3. **Reference results as data** [V]: pytest-regressions with committed reference
   files and provenance sidecars; pytest-benchmark for PR deltas; asv for history.
4. **Publication** [V JOSS/pyOpenSci]: JOSS needs ≥ 6 months of visible history
   (earliest 2027-01) and an AI-usage disclosure; the methods paper needs
   convergence at several Ha, one 3-D benchmark against an independent reference,
   gradient verification, a cost figure and a Zenodo bundle.

---

## 4. Decisions (ADR style)

Each decision records the context, the decision, its consequences, and the
reviewer's reconciliation of #64/#65/#66. New decisions go to `docs/adr/`.

**D1. One new 3-D core on the Ni/MAC/fast-diagonalisation recipe.**
Context: the generic recurrence is Stokes-like, clipped and unconverged; the B2
lane is closer to Ni's scheme but collocated, tuned per benchmark and unconverged.
Decision: write `lmx/core3d` (Section 5) from the recipe in 3.1–3.2. Before
committing the momentum discretisation, run #65's tiny-oracle comparison (MAC vs
repaired collocated on an 8³ problem: nullspaces, energy identity, adjoint cost,
line count) and record the result in `docs/adr/0002`; MAC is the default and the
oracle must show a reason to deviate. Reuse the B2 lane's face-current and
limited-convection pieces, the mesh/materials/IO modules and SOLVAX solvers.
Consequence: one formulation for duct and pipe; steady and transient share one
residual; adjoints by IFT; ≈ 2,000 lines replace ≈ 8,000.

**D2. Freeze the specialised B1/B2 lane now; delete it on a trigger.**
Trigger: the new core reproduces Shercliff/Hunt at Ha 1000 (Table I flow rates
within 0.5 %), the ANL x0 = 3 analytic case (ΔP within 3 %) and the reduced
FreeMHD B2 smoke. Until then no new work on the lane; its tests stay. Tag
`lmx-b2-lane-v1` before deletion. Keep specs, reference CSVs, provenance, wall
contracts, restart semantics and a ≤ 600-line FreeMHD comparator. This is the
middle path between #64 (delete now) and #65 (keep until replaced).

**D3. `lax.scan` time loop with per-step checkpointing; host code sees chunks.**
Day 1 of Phase 2 profiles the old code to attribute time to host syncs vs
kernels (#65's point). The new core uses `scan` from the start regardless; a
test greps `core3d`/`timeloop` for `float(`/`bool(`/`device_get` and fails on any.

**D4. Explicit precision; no import-time x64.** `lmx.enable_x64()` or a case
`dtype`; fully developed and validation runs default to float64; the 3-D core
and Q2D accept float32 with documented tolerances; fast-diagonalisation
transforms in fp32 with one fp64 refinement step for fp64 states;
`jax_default_matmul_precision='highest'`. No float32 promise at high Ha until
measured (#65).

**D5. Direct fast-diagonalisation Poisson solves for potential and pressure.**
Separable on the tensor grid with thin walls; PCG (SOLVAX) only for
non-separable cases (explicit conducting shells with jumps), wrapped in
`custom_linear_solve(symmetric=True)`.

**D6. Thin-wall boundary condition as the default wall model**, with the explicit
shell kept as an option and a check; report the difference on Hunt (c 0.05) and
B2 (`t_w/a` 0.14).

**D7. Q2D: ETDRK with exact friction, forced cases, movies.** An integrator swap,
not a new closure (#65).

**D8. Validation as data; comparators nightly.** Reference profiles as CSV/NPZ
with provenance sidecars compared by fast tests; Docker, GPU and refinement
campaigns nightly on the office box, never in PR CI.

**D9. Thermal transport as a passive scalar on the new core (Phase 5).** #65's
sequencing: conduction slab and MMS → laminar Nu limits → conjugate walls with
Joule heating → Mistrangelo 2025 target. No buoyancy before 2.0.

**D10. Design and device examples use offline-sampled fields, not live coil AD.**
Envelope, channel topology, exterior field and mesh are separate inputs (#65);
ESSOS `BiotSavart.B` evaluates one point, batch with `vmap`; VMEX interior
samples are not exterior fields.

**D11. Fully developed inverse design ships early (Phase 3b).** It needs no new
numerics and gives a user-facing result in week 2–3 (#65's "next useful
result"): eliminate the drive analytically (`f = Q_target/G`), fit a profile,
minimise work at fixed flow with checked implicit gradients.

**D12. Process: small PRs stay, ceremony goes** (Section 9), plus #65's stop
rules: before any costly campaign, log hypothesis, expected diagnostic change,
budget and exit criterion; a failed bounded attempt produces a decision, not a
longer continuation; no revival of the August B2 predictor/relaxation/Anderson
experiments without new residual evidence; no dense production Jacobian.

**D13. Releases are tied to gates, not dates.** `v2.0.0` when Phases 0–4 pass
(Q2D and thermal may ship research-stage); PyPI and Zenodo at the tag; JOSS not
before 2027-01; methods paper after Phase 6. The version exists because users
cite versions and Zenodo mints DOIs per release (#65's "release hold" becomes
"release at gates").

**D14. Reconciliation record.**

| Topic | #64 | #65 | #66 | Adopted |
|---|---|---|---|---|
| B1/B2 lane | delete now | keep until replaced | freeze, delete on trigger | D2 |
| 3-D formulation | trace loops, add convection | one residual; MAC vs collocated on a tiny oracle | new MAC core on Ni's recipe | D1 with the oracle as a recorded gate |
| Host loops | rewrite | profile first | profile day-1, new core uses scan | D3 |
| Precision | explicit dtype | explicit; no fp32 promise at high Ha | explicit; fp32 transforms + fp64 refinement | D4 |
| Poisson | – | – | fast diagonalisation | D5 |
| Walls | – | thin wall needs validity check | thin wall default | D6 with the check |
| Thermal | parked | essential, bounded laminar | passive scalar Phase 5 | D9 |
| Design example | – | first deliverable | Phase 6 | D11: Phase 3b (early, parallel) |
| Q2D | forced + movies | qualify forced example | ETDRK + cases + movies | D7 |
| GPU | campaign after tracing | measure, report crossover | targets with expected ranges | G4/G5 with crossover reported |
| Release | 2.0 at gates | hold | 2.0 at gates | D13 |
| Plan | 190 lines | 600 lines | 675 lines | this document; frozen |

**D15–D21** (2026-09-13 review) are recorded in
[`docs/adr/0005-review-2026-09-13.md`](docs/adr/0005-review-2026-09-13.md):
float32 numbers only with TensorFloat-32 off (D15); mixed precision by defect
correction through SOLVAX (D16); O(1)-memory Krylov in the steady path with
adjoint-consistent electromagnetic interpolation (D17); the thin wall as a
separable boundary row with its own second-order unknown (D18); fully
developed reporting through the affine solve (D19); tiered B1/B2 gates with
the inertialess core-flow model as the workhorse (D20); critical path first
(D21). D4's precision sentence and D2's trigger are read through them.

---

## 5. Target architecture

```
src/lmx/
  grid.py            Grid (static shape, spacing, offsets), Field pytree, stretched 1-D coordinates
  bc.py              pad-with-ghosts per BC kind: periodic, no-slip, insulating, thin-wall, symmetry
  ops.py             slice stencils on padded arrays: grad, div, laplacian, face interpolation
  poisson.py         FastDiag (host eigh, tensor apply), DCT path, PCG fallback via SOLVAX
  em.py              face currents J_n,f, Ohm's law, face Lorentz force, thin-wall surface Laplacian
  core3d.py          one step(state, params) -> state: predictor (CN diffusion, conservative limited
                     advection, implicit Lorentz damping correction), potential solve, projection
  timeloop.py        scan over checkpoint(step); chunked host acceptance, restart, logging
  steady.py          residual R(u,p,phi;theta); Newton–Krylov via solvax.root_solve; IFT adjoint
  fully_developed.py existing 2-D solver (from cases/solvers), thin-wall option, steady-linear response
  q2d.py             ETDRK2/4 spectral SM82 with forcing, budgets, spectra
  thermal.py         energy equation on the MAC grid, conjugate walls, Joule source (Phase 5)
  fields.py          analytic fringes (Votyakov), tabulated NPZ fields, offline ESSOS sampler
  shard.py           make_mesh(('x',)), slab sharding, ppermute halos, all_to_all transposes
  io.py, specs.py, cli.py, physics.py   as today, trimmed
```

Data model: one frozen `Case` dataclass (geometry, materials, field, drive,
walls, solver controls, dtype), one `State` pytree (face velocities, p, φ), one
`Result` (state, diagnostics, status). Names are labels; models are selected by
fields (already true after #63). Gauges follow boundary conditions and conductor
connectivity; a fixed-flow multiplier is an unknown in the equations, never a
post-hoc rescaling (#65).

Solver strategy: transient = projection step under `scan`; steady =
Newton–Krylov on the same residual with the projection step as preconditioner;
both share `em.py` and `poisson.py`. Adjoints: steady → IFT (one transposed
solve); transient → per-step `jax.checkpoint` in `scan` or SOLVAX
`checkpointed_fori_loop`. Never tape Krylov iterations.

Sharding: slab along the axial direction for the 3-D core (halo 1 for
second-order stencils, 2 with limited advection); Q2D via sharded `jnp.fft` or
jaxDecomp; both selected by `num_devices`. Design sweeps use `vmap` batching
before spatial sharding.

---

## 6. Phased work plan

Effort in person-weeks for one developer with agents. Tracks marked ∥ run in
parallel. Every phase ends with a PR that adds its figure to the gallery and its
rows to the validation matrix. "Implemented" closes nothing; the exit column does.

### Phase 0 — Integration and hygiene (week 1) — **complete 2026-09-07**

| Step | Action | Exit |
|---|---|---|
| 0.1 | Fix the VJP NaN in `_limited_linear_vector_face_weights_duct` (safe `jnp.where` on both branches); add resting and non-resting VJP tests; keep primal values and the forward Jacobian test | JVP/VJP finite; transpose duality and finite differences pass |
| 0.2 | Honour `max_steps` in the extruded solver; make CLI `--ny/--nz` apply to fully developed cases or reject them | a 600-step request runs 600 steps; CLI arguments have one contract |
| 0.3 | Explicit precision (D4): `lmx.enable_x64()` and case `dtype`; remove the import-time switch behind a deprecation warning | suite green in both modes |
| 0.4 | Freeze the B1/B2 lane (D2): no new work; tag `lmx-b2-lane-v1` at `main` | tag pushed; CONTRIBUTING notes the freeze |
| 0.5 | Fix local coverage source (`--cov=src/lmx`), adopt markers `unit`/`regression`/`slow`/`gpu`/`external`, split CI as in Section 9, add `docs/adr/0001-plan-adoption.md` … `0004-process.md`, `ROADMAP.md` (one page) | PR CI ≤ 8 min; nightly workflow file present (may run only when the box is up) |
| 0.6 | README as merged with this plan; examples table and gallery point to real assets | README ≤ 120 lines; quickstart executes |

Landed as #69 (0.1), #73 (0.2), #79 (0.3), #77 (0.4), #80 with #84 (0.5) and
#72 (0.6). Measured at the close: the reverse derivative is finite at rest and
in flow, a 600-step request runs 600 steps, the suite is green with x64 on and
off, `lmx-b2-lane-v1` is tagged, and the pull-request lane runs four disjoint
jobs (unit 2m02s; regression fringing, physics 4m16s and support 2m41s) inside
the eight-minute target with the pinned FreeMHD comparison at 5m11s.

### Phase 1 — The new 3-D core (weeks 2–5, ∥ with 2, 3, 3b)

| Step | Action | Exit |
|---|---|---|
| 1.1 | `grid.py`, `bc.py`, `ops.py`: MAC layout, stretched coordinates (tanh/geometric), pad-then-slice BCs; identities on constants/linear fields; adjoint identity `⟨div u, p⟩ = −⟨u, grad p⟩` with volume weights; projection orthogonality and contraction (from `64be572`) | identities to 1e-14 |
| 1.2 | `poisson.py`: fast diagonalisation for Neumann/Dirichlet/periodic axes on non-uniform grids, DCT on uniform Neumann axes, PCG fallback; compare with dense on 8³; mean subtraction and nullspace cutoff | residual 1e-12 fp64; ≈ 4 kernels per solve |
| 1.3 **done** | `em.py`: face currents, potential Poisson from the same flux, face Lorentz force, thin-wall surface Laplacian with corner continuity, explicit shell option; `Σ_f J_n,f s_f = 0` per cell; Hartmann/Shercliff/Hunt currents | charge residual 1e-13; Hunt wall current within 1 % of the series solution |
| 1.3b | Second-order thin wall as a direct solve (D18): the wall potential `φ_w` becomes an unknown on the wall surface with the symmetric half-cell flux `σ(φ_P − φ_w)/(h_P/2)`, a five-point surface Laplacian `∇_τ·(c∇_τ φ_w) = j_n` and corner cells that conserve charge between the two sheets; the potential solve stays direct, either by folding the wall rows into the one-dimensional operators (Krasnov et al. 2023, per axial Fourier mode) or by a per-mode capacitance correction of the fast solve (`solvax.low_rank_corrected`); the inner GMRES of `_solve_potential` goes; a thick wall is one potential domain with a conductivity jump and harmonic-mean face conductivity | manufactured `φ_w` converges at second order; the potential operator is symmetric to 1e-12; Hunt `c` 0.01 within 0.3 % of Smolentsev Table I at Ha 500–5000; the conducting pipe within 0.5 % at ≤ 48 radial cells; the potential solve ≥ 10× faster than the Krylov route at Ha 100 on 48²; `c = 0` bit-identical to the insulating duct |
| 1.4 | Tiny-oracle comparison (D1): MAC vs repaired collocated on 8³: nullspaces, discrete energy identity, adjoint cost, lines; record in `docs/adr/0002-momentum-placement.md` | ADR merged before 1.5 |
| 1.5a **done** | `core3d.py`: projection step with the implicit Lorentz damping correction, explicit viscosity and a reported `diffusive_step_limit`; wall and periodic face constraints imposed before any divergence | **met**: plane-channel Poiseuille at second order, rates 2.01 and 2.04 over 8/16/32 cells |
| 1.5b | Implicit (Crank-Nicolson) viscosity so the step is no longer bounded by the mesh; conservative limited advection; manufactured solutions derived independently of the discrete operators, on a non-uniform mesh | observed second order with convection on; step independent of the diffusive limit |
| 1.5c | Reconcile the new core with `solve_fully_developed_fields`: match geometry, wall placement and drive normalisation, then compare; add an outside reference (`validation/shercliff.py`, Chebyshev collocation of the coupled duct system) so a disagreement can be attributed to one route rather than argued between two | the two routes agree within their discretisation error, with the convention difference documented; both agree with the spectral reference and the new core converges to it at second order |
| 1.6 | `timeloop.py`: `scan` + checkpoint; chunked acceptance; restart; energy budget on one control volume (pressure/body work, kinetic storage, viscous and Joule dissipation, electrical boundary work; no double counting of an imposed pressure gradient) | no host sync in the loop; mechanical defect 1e-13 at the steady state; the Lorentz force does exactly minus the Joule dissipation on a uniform mesh (1e-12) and to first order through a conducting wall; a run in two chunks equals the same run in one to 1e-14 |
| 1.7 **done for the primal** | `steady.py`: residual; Newton–Krylov via SOLVAX `root_solve` with explicit matrix-free tangent/transpose solves; projection-step preconditioner; IFT adjoint; reject failed primal/adjoint eagerly or invalidate traced fields/derivatives without callbacks (#121); compiled objectives (#123) | Shercliff Ha 0–1000 within 2 % of `validation/shercliff.py` (1.3 % at Ha 20, 0.83 % at Ha 100, 1.0 % at Ha 300, 0.5 % at Ha 1000 on the test mesh); adjoint vs finite differences 1e-6 **measured at Ha 5 only**: the adjoint fails from Ha 300 (1.7c). No geometry/material derivative claim |
| 1.7c | Precondition the tangent and transpose solves in `steady._krylov` with the primal projection preconditioner (its adjoint for the transpose); add adjoint-versus-central-difference regressions at Ha 100 and 300 to the regression tier and Ha 1000 to the slow tier | Ha 300 on 48²: gradient finite and within 1e-6 of central differences (measured 7.8e-9), 21.5 s against 38 s; Ha 1000 on the test mesh still fails with GMRES(100)×60 and is the gate of 1.7e |
| 1.7d | Adjoint-consistent electromagnetic interpolation (D17): the face electromotive force and the cell-to-face force interpolation become exact discrete adjoints on stretched and polar meshes; then the Stokes-limit steady solve runs preconditioned CG in the face-volume inner product and advective cases run `solvax.gcrot` with restart ≤ 60 | ohmic identity exact to 1e-12 on stretched and polar meshes (today first order); steady operator asymmetry ≤ 1e-12 (today 3.7e-2); a 3-D 128³ steady solve within 16 GB with peak memory recorded; iteration counts at Ha 20–300 no worse than the measured CG counts 25 / 84 / 223 |
| 1.7e | Ha-robust preconditioning, measured not promised: line-implicit relaxation in the wall-normal direction, semicoarsening multigrid (`solvax.semicoarsening_hierarchy`), and an inner Stokes–Lorentz PCG, each with Ha continuation and Krylov recycling; `solvax.pseudo_transient_continuation` (SER) as the globaliser; the mesh envelope of `duct_problem` at Ha 1000 reported | one preconditioner adopted with its iteration curve over Ha 20–1000 in the plan log; `lmx.duct_problem(hartmann=1000)` converges from rest; the Ha 1000 adjoint certifies (G11) |
| 1.7b | Hunt's thin conducting wall in the steady path: the Walker condition `j·n = ∇_τ·(c ∇_τ φ_w)` as a boundary closure on the potential, alongside the insulating one | Hunt at Ha 20, c 0.027 and 0.1, within 2 % of an extended `validation/shercliff.py` on 32 cells (1.0 % and 0.5 % measured); c = 0 reproduces the insulating duct bit for bit |
| 1.8a | The polar metric in `lmx.grid`: radial, azimuthal and axial face areas, the annular cell volume, and the `r·dtheta` azimuthal spacing that `lmx.ops` differences with. The separable stencils refuse it rather than answering wrongly | a cylinder integrates exactly; the flux-form Laplacian is exact on `1 - r²` away from the wall closure and second order on a harmonic field away from the axis |
| 1.8b | The polar Poisson: an FFT in the azimuth makes each mode `m` separable in `(r, z)` with an `m`-dependent radial operator, so fast diagonalization still applies, one eigendecomposition per mode | inverts its own operator to 1e-11, as the Cartesian one does |
| 1.8c | `lmx.pipe`: the fully developed pipe as the axial momentum equation coupled to the potential, with `B_r = B cos θ` and `B_θ = -B sin θ` giving the face electromotive force, one preconditioned Krylov solve because the system is linear | the Ha 0 limit reproduces Poiseuille `Q/A = 1/8` at order 2.00; the azimuthal discretization is second order at Ha 20; charge is conserved to 1e-10; a conducting wall reduces the flow rate monotonically |
| 1.8d **done** | `validation/pipe.py`: Fourier in the azimuth and Chebyshev on the diameter, the double-cover representation that removes the axis rather than treating it | **met**: exactly `1/8` at zero field; the insulating pipe within 0.04 %, 0.15 %, 0.37 % and 0.43 % at Ha 0, 5, 20 and 100; the conducting wall converging at order 2.5–3.0 to 0.43 % at 96 radial cells |
| 1.9a | Spatially varying imposed field `B(x)`: tabulated or analytic, curl- and divergence-free, on cell centres and faces, used by the electromotive force and the Lorentz force; the fringe of Section 3.1 item 7 and Votyakov's solenoidal pair | discrete `∇·B` to 1e-14; energy identity holds with a varying field; the Hartmann limit is recovered station by station |
| 1.9b | Non-periodic axial direction: developed inflow, `∂p/∂x → 0` and `∂φ/∂x → 0` outflow, buffers of 10 half-widths upstream and 8 downstream (ANL development lengths at Ha 1000), and the flow-rate multiplier as an unknown of the steady system | mass and charge balance to 1e-12; fixed flow held to 1e-10 by the solve, never by rescaling |
| 1.9c | Inertialess core-flow model (D20) on the same grid, wall operators and field provider: core pressure and the two wall potentials as three coupled 2-D elliptic equations on the wall surface (Kulikovskii; Hua & Walker; Bühler), differentiable, general wall conductance | ANL fringe 0.0932 and fully developed 0.0754 within 1 %; Walker's fully developed limits; adjoint checked against central differences |
| 1.9d | Validation rows 6–7 on the 3-D core (ANL fringe in the Stokes limit on three meshes; Ni fringing case at Ha 100, Re 200) with continuation in N, fitted as `Δp∞ + αHa^−1/2 + βN^−1/3` | ANL within 1 % of 1.9c; Ni case within 3 %; the inertial share of the excess ΔP reported |
| 1.10a | Put the new core in the public API: `ChannelProblem`, `duct_problem`, `solve_steady_state`, `advance`, and `solve()` dispatching a `ChannelProblem` | a duct is solved from `import lmx` alone, in three lines, with the mesh chosen from the Hartmann number |
| 1.10b | Route the fully developed `CaseSpec` path through the new core after 1.7c–1.7e and 1.11 (today the new core is 2.2–13× slower than the legacy solve on the same duct); old generic and lane paths unreachable from the public API; the seven examples and `design.py` migrated | new core warm time ≤ legacy warm time on Shercliff/Hunt Ha 20–1000 at matched accuracy; suite green; no example slower; architecture audit ≤ 14 modules after Phase 4 |
| 1.11 | Mixed precision (D16): `FastDiagonalPoisson`/`FastDiagonalHelmholtz` solve in float32 with float64 residual correction through `solvax.iterative_refinement` and `solvax.as_low_precision`, one or two sweeps chosen from the measured contraction; eigenbases cast once at factorization; matmul precision pinned (D15); float32 states keep the float32 solve | float64 error ≤ 1e-10 relative on 64³–192³ wall-resolving meshes (measured 2.0e-11 at 128³, 2.2e-11 on the Ha 1000 layer mesh); A4000 step 64³ / 128³ / 192³ from 8.3 / 55 / 270 ms to ≤ 4 / 16 / 65 ms (measured 3.5 / 13.8 / 59.8 ms with trajectories within 9e-11 of float64) |
| 1.12 | Partial diagonalization, only on a measured gain: FFT on the periodic axis, DCT-II on uniform Neumann axes, dense eigenbasis on stretched axes and `solvax.tridiagonal_solve` on one stretched axis, for the Poisson and staggered Helmholtz factorizations and the polar radial solve; the transform types follow the Schumann–Sweet table | the assembled operators are diagonalized to 1e-13 (verified); measured 0.84–0.89× of BLAS on CPU at ≤ 96³ and 0.01× on the A4000 with `jax.lax.linalg.tridiagonal_solve` (batched cuSPARSE: 566 ms against 3.0 ms at 64³), so the only candidate is a one-thread-per-column Thomas sweep as CaNS and Oceananigans run it (`solvax.tridiagonal_solve(method='thomas')` or a `vmap`ped scan), gated on ≥ 1.5× over the mixed-precision dense solve of 1.11 at 128³–192³; otherwise record and close |

Effort ≈ 3.5 weeks.

### Phase 2 — GPU and scaling (weeks 2–6, ∥; needs the office host)

| Step | Action | Exit |
|---|---|---|
| 2.0 | Precision provenance (D15): `lmx.enable_x64()` and the case `dtype` pin `jax_default_matmul_precision` (`highest`); `scripts/run_benchmarks.py` records matmul precision, TF32 override, XLA flags, driver, CUDA and host load, and refuses float32 JSON without them; retract "1/64" here, in the README and in `docs/validation`; rename the JSON provenance to rewritten-history commits | float32 contraction error ≤ 1e-6 relative on the A4000 (measured 2.6–6.1e-7); every JSON carries the precision |
| 2.1 | Controlled single-device campaign on an idle card: alternating order, two discarded warm-ups, ≥ 10 repeats, median with a bootstrap interval, float64 / true float32 / TF32 / mixed (1.11); one `jax.profiler` trace per case with `jax_transfer_guard='disallow'` | replaces the six JSON files; G4 restated from these medians; host syncs listed from the trace |
| 2.2 | Benchmark harness `scripts/run_benchmarks.py`: the staggered 3-D core at 32³–192³ and Q2D at 256²–2048², × {fp32, fp64} × {CPU, 1 GPU, 2 GPU}, writing JSON with host, device, versions, dtype, tolerances and an accepted flag; `scripts/make_showcase_figures.py --only scaling` draws it from that JSON and measures nothing itself | reproducible JSON + one figure script |
| 2.3 | Nightly cron on the office box (Section 9), trusted `main` only: slow tier plus the 2.1 matrix, results to a `benchmarks` branch, failures to one issue (the open half of #75) | first scheduled run observed, not the schedule installed |
| 2.4 | Sharding, measured first: place the array across devices and see what the partitioner does unaided, which is the baseline any hand-written decomposition has to beat | Q2D placement is functional; speedup remains unmet. The staggered core has a separate obstacle: its three components have length `n+1` on their own axis and `n` on the others, so no single axis divides evenly across all three |
| 2.4a **done** | Compile the shared Q2D setup so constants follow input placement without an explicit NumPy host round-trip | PR #108: eight 64²/512² × fp32/fp64 × one/two GPU cases preserve baseline fields and diagnostics exactly. Warm timings are mixed, not a universal speedup. Two/four logical CPU-device checks include fields, diagnostics and analytical friction derivatives. CPU placement is a shared-host test, not fixed-core strong scaling. FFT all-gathers are present; runtime attribution is still needed (#107) |
| 2.4a′ | G5a first: ensembles across the two cards through `solvax.shard_batch` for Ha sweeps, design objectives and gradient checks; one padded common layout for the staggered components so a batch shards evenly | ≥ 0.9 efficiency on two A4000s for a Ha sweep; fields identical to the single-card run |
| 2.4b | G5b, only where a run does not fit one card (≥ 384³ in float64 on 16 GB) or when 2.4a′ leaves a demonstrated need: slab in one axis with `shard_map`, `ppermute` halos and `all_to_all` transposes for the transforms (jaxDecomp's pattern), the tridiagonal axis kept local. The two A4000s are PCIe-joined with no NVLink (`nvidia-smi topo -m` reports `NODE`), which is the bandwidth such a design has to answer to | 1-GPU vs 2-GPU fields agree to 1e-12 (fp64); efficiency reported at 128³, 256³ and the memory-driven sizes |
| 2.5 | Compile hygiene and optimisation to targets: factorizations as pytree arguments, scalar constants instead of full arrays in `_constant`, persistent compilation cache, `jax_explain_cache_misses` in CI, fusion audit, buffer donation; report crossover size | cold 2-D steady solve ≤ 5 s; zero retraces across a Ha sweep; G4/G5 numbers, or the measured reason they are not met |

Effort ≈ 2 weeks spread across the phase.

Logical CPU devices share cores and memory, so they qualify placement
correctness only; G5 stays open until fixed-resource measurements exist.
Performance PRs name the profiled share of runtime they target (≥ 10 %) and
the measured change; trims below that are batched (D21).

### Phase 3 — Q2D turbulence (weeks 2–4, ∥)

| Step | Action | Exit |
|---|---|---|
| 3.0 | Real transforms: `rfftn`/`irfftn` in `_flow` and `_nonlinear` (the dealiased nonlinear term is identical to 5.7e-16) | ≥ 1.5× per stage measured (1.54–2.52× on CPU); fields unchanged to round-off |
| 3.1 | ETDRK2/4 with Kassam–Trefethen coefficients by the 32-point half-circle mean; friction and viscosity in the exponential; 2/3 dealiasing; energy/enstrophy budgets and spectra as scan outputs; the `k^-3` range measured as a fitted slope, not drawn as a guide line | Taylor–Green decay exact to 1e-10; budgets close to 1e-8 |
| 3.2 | Cases 12–15 of Section 7 (frozen decay, vortex lattice, Kolmogorov + drag, Tithof flow); two seeds; averaging-window uncertainty; grid/time sensitivity | numbers reproduced within stated tolerance |
| 3.3 | Cross-check against GeophysicalFlows.jl or Dedalus at 256² with identical initial data | relative L2 ≤ 1e-3 |
| 3.4 | Movies: 1024² decay with merging, forced lattice sweep, Kolmogorov condensate; README animation ≤ 500 KB; MP4s as release assets | three movies produced on GPU |
| 3.5 | Regime statement in the tutorial (t_H formula, n, Re/Ha < 380, N ≫ 1, PbLi numbers); the demo becomes a forced case | tutorial updated; `q2d_turbulence_demo.py` forced |

Effort ≈ 1.5 weeks.

### Phase 3b — Fully developed inverse design (weeks 2–3, ∥; D11)

| Step | Action | Exit |
|---|---|---|
| 3b.1 | Drive for requested throughput: the fully developed problem is linear in the force density; compute `G = Q(f = 1)`, eliminate `f = Q_target/G`, check `df/dQ = 1/G` and two direct solves | exact to solver tolerance; #117 trims quadrature to mesh-only weights, with exact rectangular/layered weights and compiled velocity gradients; 12 affected design tests pass. This removes unused material construction, not the cost of the compiled flow solve |
| 3b.2 | Profile fit: minimise area-weighted squared error over positive drive and bounded field multiplier with `solve_fully_developed_fields`; recover synthetic parameters; report irreducible error for incompatible targets | Executable fully-developed tutorial and regression: 12² Shercliff synthetic recovery within 1e-6, implicit gradients checked against finite differences; antisymmetric target tests the irreducible residual. Identifiability limits documented. Broader Hunt/gallery extension remains 3b.4 |
| 3b.3 | Minimum work at fixed flow over wall conductance and aspect ratio with implicit gradients; feasibility and projected-gradient/KKT checks; re-solve the optimum on a held-out finer mesh. **After 1.10b**, on the new core, with geometry derivatives through the metric; not on the legacy stack (D21) | Taylor and finite-difference sweeps; finer-mesh confirmation |
| 3b.4 | Extend `hunt_example.py` (no new optimiser API); figure: profiles before/after, objective and constraint iterates, gradient error, work vs flow. Reporting through the affine solve (D19): `solve_fully_developed_fields` certifies the linear problem it already solves, the pseudo-time loop is kept as a transient only and loses its per-step host sync; #113 closes on that, not on a longer cold-start campaign | cold Hunt Ha 20 residual ≤ 1e-10 relative and the field within 1e-10 of an independent sparse solve (today the loop stalls at an update of 1.4e-9 and its field is 1e-8 off); figure in the gallery |

Effort ≈ 1 week.

### Phase 4 — B2 validation and lane retirement (weeks 6–9)

| Step | Action | Exit |
|---|---|---|
| 4.1 | Digitise Smolentsev 2015 Figs 3–4 and ANL/FPP/TM-228 Figs 11–13 including the measured `B(x)` (two independent passes; axis calibration recorded); CSV + provenance sidecar with uncertainty per ASME V&V 20 (`u_D` from digitisation and the field); request the original files from the author; freeze one curl- and divergence-free field model and the pressure-tap geometry with the gate | uncertainty column filled; field model and tap geometry recorded |
| 4.2 | Matched FreeMHD B2 case in `freemhd_install` (blockMesh with stretching, the frozen field, the 0.66 cm wall as a solid region since FreeMHD has no thin-wall patch, `endTime` ≥ 20 transit times); Shercliff/Hunt demos with physical run length and L2 assertions | FreeMHD B2 curve stored as reference data with image ID and hashes, or the recorded cost that stopped it |
| 4.3 | B2 through the core-flow model (1.9c) with the measured field and the tap correction; the 3-D core at moderate N as a cross-check with continuation in N (1.9d); thin wall vs thick wall (`t_w/L ≈ 0.15`); `B_y`-only vs solenoidal field; refinement, conservation, tolerance and iteration independence | axial pressure gradient within 5–10 % of experiment; transverse Δp by shape and sign; the inertial share and the wall-model difference reported |
| 4.4 | Code-to-code: LMX core-flow model, LMX 3-D core and FreeMHD on the ANL fringe and on B2 at moderate N | within 1–2 % of each other, or `external_validation_open` with the numbers |
| 4.5 | B1 through the core-flow model (its regime, N 10,700); KIT MEKKA pipe entry (Bühler et al. 2020; pressure and wall potential) as the second experimental target; a resolved 3-D B2 only as a cost-bounded cross-check after 1.7d–1.7e, with the reason recorded if not attempted | documented either way |
| 4.6 | Delete the B1/B2 and generic extruded lanes (D2 trigger read through D20: 1.9c–1.9d pass and 1.10b is at parity); collapse `validation/freemhd.py` + parity script into `lmx/freemhd.py` ≤ 600 lines; move lane tests to the tag; remove test-only helpers | −≈ 6,000 source lines and the legacy fully developed modules; audit ≤ 14 modules and ≤ 9,000 lines; tests ≤ 1.2× source; tag exists |

Effort ≈ 2.5 weeks plus Docker/GPU time.

### Phase 5 — Thermal passive scalar (weeks 8–10)

| Step | Action | Exit |
|---|---|---|
| 5.1 | `thermal.py`: `ρc_p(∂_t T + u·∇T) = ∇·(k∇T) + q_vol + J²/σ`, constant properties, conjugate walls (T and normal-flux continuity; optional contact resistance), inlet/outlet conditions; axial conduction when Pe requires; integrated in the scan | conduction slab and advection–diffusion MMS at second order |
| 5.2 | Laminar limits: Nu = 48/11 (uniform flux) and 3.66 (uniform T) for the pipe; rectangular duct values (Shah & London); bulk temperature by enthalpy-flux weighting | within 0.5 % |
| 5.3 | Heated Hunt duct with Joule heating; balance of inlet/outlet enthalpy, heat input, Joule and viscous heating, storage | balance to 1e-6 |
| 5.4 | Mistrangelo et al. 2025 heated conducting duct (Ha 235) | comparison figure; deviations explained |
| 5.5 | First thermal design: minimise maximum wall temperature or hydraulic power subject to heat removal, temperature, throughput and geometry constraints; distinguish fixed-flow from fixed-pump-power comparisons | feasible candidate confirmed on a finer mesh |

Effort ≈ 1.5 weeks.

### Phase 6 — Device examples (weeks 9–11)

| Step | Action | Exit |
|---|---|---|
| 6.1 | Field provider contract: `B(X, parameters)` in tesla at Cartesian metres, explicit parameter PyTree, validity domain, out-of-domain rejection, divergence/curl checks in the source-free region; analytic, tabulated NPZ (≤ 200 KB, provenance) and offline ESSOS sampling | tests on straight-wire and loop limits, rotations, refinement |
| 6.2 | Stellarator channel: straight channel outside the plasma of a chosen equilibrium (VMEX `examples/data/input.LandremanPaul2021_QH_reactorScale_lowres` or the ESSOS example coil set); first-wall gap, layer thicknesses, clearance and self-intersection checks; PbLi properties; Ha/N/Re reported | 3-D pressure drop and current-loop movie |
| 6.3 | Gradients of pressure drop and peak wall current w.r.t. field scale, wall conductance, geometry scale and flow via the IFT adjoint; one bounded optimisation; held-out finer-mesh re-solve | adjoint vs finite differences; feasible optimum confirmed |
| 6.4 | Tokamak-like (1/R field, Solov'ev seed) and mirror (two-coil, Pleiades reference) variants of the same script | one script, three configurations |

Effort ≈ 1.5 weeks. First claim: device-informed channel screening, not an
optimised blanket.

### Phase 7 — Release (week 12)

| Step | Action | Exit |
|---|---|---|
| 7.1 | `v2.0.0`, CITATION.cff, Zenodo integration, PyPI upload, release assets (movies, benchmark JSON, figures, digitised data) | clean-install smoke on 3.10 and 3.13 |
| 7.2 | Docs pass: Diátaxis structure; each capability links equation → implementation → example → validation | Sphinx `-W`; gallery complete |
| 7.3 | Methods-paper outline with figures mapped to release assets | `docs/paper/outline.md` |

### Dependency graph

```
Phase 0 ─┬─ Phase 1 (core) ──┬─ Phase 4 (B2, lane deletion) ─┬─ Phase 7
         ├─ Phase 2 (GPU)  ──┤                               │
         ├─ Phase 3 (Q2D) ───┤                               │
         └─ Phase 3b (design)┘   Phase 5 (thermal, after 1.7) ┤
                                 Phase 6 (device, after 1.7 and 6.1) ┘
```

### Order of work from 2026-09-13

The critical path is 1.7c → 1.7d → 1.7e → 1.10b → 4.6, with 1.11, 1.3b,
1.9a–1.9d and 2.0–2.1 in parallel worktrees. First pull requests, each one
concern and one page:

| # | Step | Size | Evidence attached |
|---|---|---|---|
| 1 | merge #123 on its exact head | **done** (`b5dbb06`) | CI |
| 2 | 2.0: pin matmul precision, record it, retract "1/64" in plan, README and `docs/validation`; fix the stale README claims of Section 2.2 | **done**, #126 | the 2026-09-13 precision logs |
| 3 | 1.7c: preconditioned tangent and transpose solves; Ha 100/300 adjoint regressions | **done**, #127 | Ha 300 gradient against central differences |
| 4 | 3b.4 / D19: affine reporting; #113 closed | **done**, #129 | cold Hunt residual and field check |
| 5 | 1.11: mixed precision through SOLVAX | **done**, #128 | CPU and A4000 JSON with precision recorded |
| 6 | 3.0: Q2D real transforms | **done**, #125 | per-stage timing |
| 7 | 2.1: controlled campaign on an idle card; 2.3 nightly observed | in progress, #142 | protocol record |
| 8 | 1.7d **done** (#131, #135); 1.3b **done** (#136); 1.7e **done** (#137); 1.9a **done** (#144); load-robust steady tests **done** (#132) | one PR each | their exit columns |
| 9 | #145 (varying-field certification) and #146 (pipe wall force) in parallel; then 1.9b (non-periodic axial direction, flow-rate multiplier) and 1.9c (core-flow model); 1.10b routing once #141 confirms parity | one PR each | the issues' exit criteria; 1.9b/1.9c rows |

Everything else waits (D21): no new features on the legacy fully developed
stack, no Q2D trims below a profiled 10 %, no thermal or device work before
1.10b.

---

## 7. Validation ladder

| Row | Case | Parameters | Reference | Tolerance | Phase |
|---|---|---|---|---|---|
| 1 | Poiseuille, B = 0 | rect duct, pipe | series / exact | 1e-8 | 1.5 |
| 2 | Hartmann | Ha 10–10⁴ | analytic | 1e-6 on three meshes | 1.7 |
| 3 | Shercliff | Ha 20–10⁴ | Table I flow rates (Smolentsev 2015) | 0.5 % | 1.7 |
| 4 | Hunt | Ha 20–10⁴, c 0.01–0.1 | series; Table I | 0.5 %; wall current 1 % | 1.7 |
| 5 | Conducting pipe | Ha 100–6600, c 0.027 | Chang–Lundgren / Gold | 0.5 % | 1.8 |
| 6 | Ni fringing case | Ha 100, Re 200, Ni eqs. 87–88 field | Ni 2007 Figs 16–17 | ΔP 3 % | 1.9d |
| 7 | ANL fringe (inertialess SOR solution, 0.1 % stopping) | square duct, x0 = 3, c = 0.02 | ΔP_total 0.0932, fd 0.0754 | 1 % (tighter is meaningless) | 1.9c, 1.9d |
| 8 | Manufactured 3-D | non-uniform grid, convection on | MMS | second order | 1.5 |
| 9 | FreeMHD Shercliff/Hunt | Ha 20–1000 | long-run FreeMHD | 3 % | 4.2 |
| 10 | ALEX B2 | Ha 2900, N 540, c 0.07, t_w/L 0.15 | digitised experiment (figures only; tap through the side wall) + FreeMHD | axial gradient 5–10 % of experiment via the core-flow model; transverse Δp shape and sign; 1–2 % code to code | 4.3, 4.4 |
| 11 | ALEX B1 | Ha 6600, N 10700, c 0.027 | digitised experiment | core-flow model; 3-D only if affordable | 4.5 |
| 12 | Q2D frozen decay | ν → 0, γ t_eddy 0.1–1 | exact rescaling identity | 1e-8 | 3.2 |
| 13 | Q2D vortex lattice | k_f = 6, Re_α 1–30 | Sommeria 1986: C 3–7, condensation | C in range | 3.2 |
| 14 | Q2D Kolmogorov + drag | sin(4y), γ 0.1, Re 1000/4000 | Kochkov 2021 fields | spectra | 3.2 |
| 15 | Q2D Tithof flow | α 0.064, β 1 | Re_c 7.60, q_c 0.465 | 2 % | 3.2 |
| 16 | Q2D cross-code | 256², identical initial data | GeophysicalFlows.jl / Dedalus | 1e-3 | 3.3 |
| 17 | Fully developed design | Hartmann, Shercliff, Hunt | analytic response, synthetic recovery | 1e-6; finer mesh | 3b |
| 18 | Thermal limits | pipe/duct laminar | Nu 48/11, 3.66; Shah & London | 0.5 % | 5.2 |
| 19 | Heated conducting duct | Ha 235 | Mistrangelo 2025 | figure | 5.4 |
| 20 | Derivatives | every continuous input | FD sweeps, Taylor, duality | 1e-6 fp64 | 1.7, 3.1, 3b, 6.3 |
| 21 | Performance | 3-D 64³–256³; Q2D 512²–2048²; matmul precision recorded | JSON + figure | G4/G5 | 2.1, 2.5 |
| 22 | Hunt, conducting Hartmann walls | Ha 500–5000, c 0.01 | Smolentsev 2015 Table I (A2) | 0.3 % | 1.3b |
| 23 | KIT MEKKA pipe entry | Ha 2000, N 200, c 0.071 thick wall | Bühler, Brinkmann & Mistrangelo 2020 (digitised) | stated uncertainty | 4.5 |
| 24 | Core-flow model vs 3-D core | ANL fringe; B2 at moderate N | each other | 1 % | 1.9d |
| 25 | Mixed precision | 64³–192³ wall-resolving | float64 solve | 1e-10 relative | 1.11 |
| 26 | Steady adjoint at high Ha | Ha 100, 300, 1000 | central differences | 1e-6 | 1.7c, 1.7e |

Report Richardson/GCI only where refinement is asymptotic; separate numerical,
digitisation, measurement and model-form uncertainty; agreement of two codes is
not validation against nature.

---

## 8. Showcase figures and movies

All produced by `scripts/make_showcase_figures.py` at demo settings on CPU and
at production settings on the office GPU. Git media budget ≤ 1 MB; movies and
raw data as release assets with checksums.

| Asset | Content | Budget | Status |
|---|---|---|---|
| `docs/_static/q2d_turbulence_256.webp` | 256² decaying Q2D turbulence, 30 frames | 480 KB | in this PR |
| `docs/_static/q2d_turbulence_poster.webp` | three snapshots + energy spectrum with k^-3 guide | 100 KB | in this PR |
| `docs/_static/hunt_side_layers.webp` | Hunt profiles Ha 20–1000, 2-D map at Ha 1000, jet position vs Ha^-1/2 | 44 KB | in this PR |
| `docs/_static/device_scaling.webp` | Time per step against size for the 3-D core and Q2D, CPU and one GPU, both precisions, with the G4 targets drawn | 50 KB | in this PR |
| `docs/_static/pipe_flow.webp` | Pipe radial profiles along and across the field at Ha 0–400, the cross-section at Ha 400, and `Q/A` against `Ha^-1` with the Poiseuille limit | 47 KB | in this PR |
| `docs/_static/validation_ladder.webp` | Hartmann-layer collapse onto `1 - e^-ξ`, flow-rate error against `validation/shercliff.py` for c = 0, 0.027, 0.1 at Ha 20–300, and mesh convergence | 43 KB | in this PR |
| Fully developed design | profiles before/after, iterates, work vs flow | ≤ 100 KB | Phase 3b |
| 3-D fringing duct | current loops and axial pressure at Ha 500, 128×64×64 | ≤ 150 KB + MP4 | Phase 1.9 |
| Q2D forced lattice and condensate | 1024², movie | 300 KB animation + MP4 | Phase 3.4 |
| GPU scaling | time/step vs size; CPU / 1 GPU / 2 GPU; fp32/fp64 | 60 KB | Phase 2.5 |
| B2 comparison | ΔP curves: LMX three meshes, FreeMHD, experiment with error bars | 80 KB | Phase 4.4 |
| Heated duct | temperature and Nu | ≤ 100 KB | Phase 5 |
| Stellarator channel | coil field along the channel, ΔP, wall current | ≤ 150 KB + MP4 | Phase 6.2 |

Figure rules: resolved profiles (≥ 20 points for smooth curves), discrete
controls drawn as points, three meshes for convergence, infeasible or failed
iterates marked, fixed colour scales and physical time in movies.

---

## 9. How we work

- **Branches and PRs.** `main` always releasable; short-lived branches; merge
  commits for integration batches, squash otherwise; ≤ 400 changed lines per PR,
  one concern, tests in the same PR; description = what, why, how verified
  (three lines) plus the plan step it advances. No per-PR evidence template.
- **Tests.** Markers `unit`, `regression`, `slow`, `gpu`, `external`. PR CI runs
  `unit` on one Python (≤ 5 min) and `regression` only for PRs to `main`
  (≤ 8 min); no coverage threshold on PRs; coverage reported at release.
  Reference data via pytest-regressions with provenance sidecars. Docs-only PRs
  run docs checks only.
- **Nightly (office box, cron, never a PR runner).** `git pull main`; `slow`,
  `gpu`, `external` (FreeMHD Docker); benchmark JSON to a `benchmarks` branch;
  failures open one issue.
- **Benchmarks.** pytest-benchmark on PRs carrying a `run_benchmarks` label; asv
  history from the nightly.
- **Campaign discipline (D12).** Before any run longer than an hour: hypothesis,
  expected diagnostic change, budget, exit criterion, in the issue. A failed
  bounded attempt produces a decision. Tolerances are specified from scales,
  refinement and uncertainty before running, never relaxed to obtain a pass.
- **Planning.** `ROADMAP.md` one page (next three milestones, dates); ADRs in
  `docs/adr/` for decisions that change equations, tolerances, formats or public
  API; this `plan.md` frozen except at phase boundaries.
- **Size.** Architecture audit budgets follow this plan (modules ≤ 14 after
  Phase 4, source ≤ 9,000 lines, media ≤ 1 MB, clone ≤ 5 MB); no history rewrite
  unless a measured size problem appears. Until step 4.6, the line budgets in
  `scripts/audit_architecture.py` are interim ratchets set to the merged size
  plus in-flight steps that have named their lines (2026-09-13: 18,800 total and
  10,300 maintained core, after #125–#132 added 652 lines, 274 of them in the
  `cases.py` path that 4.6 deletes, and 1.7d part ii's 144). A PR that raises a
  ratchet names its plan step and the lines it adds; 4.6 resets both to the
  Phase 4 targets.
- **AI-assisted work.** Same rules; the reviewer reads the diff; CONTRIBUTING
  carries the disclosure JOSS requires.
- **Authorship.** All commits authored by the project owner; no tool attribution.
- **Critical path (D21).** At most one open PR outside Phases 1, 2 and 4 until
  1.10b and 4.6 land; no feature lands on the legacy fully developed stack.
- **Profile before optimising.** A performance PR names the traced share of
  runtime it targets (≥ 10 %) and the measured change; a GPU number without its
  matmul precision and host load is not quoted.
- **Descriptions.** One page: what, why, how verified, plan step. Evidence is
  attached JSON and logs; handoffs update Section 2 and the log, not a new
  essay per PR.

---

## 10. Risks and fallbacks

| Risk | Mitigation |
|---|---|
| Office GPU unavailable for weeks | Phases 1, 3, 3b, 5, 6 are CPU-developable; Phase 2 targets slip, not the release |
| Fast diagonalisation inapplicable with explicit conducting shells | PCG fallback (D5); thin wall is the default |
| Newton–Krylov stalls at B2 | pseudo-transient continuation from the projection loop; report `external_validation_open` with numbers; no retuning campaign |
| Tiny oracle favours collocated | follow the ADR; the recipe's other parts (face currents, thin wall, fast diagonalisation, scan) are unchanged |
| B1 unaffordable | documented estimate; not a release blocker |
| Digitised data disputed | request originals; publish digitisation error; keep FreeMHD comparison independent |
| Rate limits on agent sessions | phases are split into independently mergeable steps |
| Plan drift | change only by ADR; competing drafts are closed |

---

## 11. References

Numerics: Ni et al., JCP 227:174 (2007) [PDF](https://bpb-us-w2.wpmucdn.com/research.seas.ucla.edu/dist/d/39/files/2019/08/JCP-v227-NiCurrentPart1.pdf); JCP 227:205 (2007); JCP 231:281 (2012) · Hua, Walker, Picologlou, Reed, ANL/FPP/TM-228 (1988) [OSTI](https://www.osti.gov/servlets/purl/6789158) · Smolentsev et al., FED 100:65 (2015) [PDF](https://www.scipedia.com/wd/images/b/b8/Draft_Samper_360028846_6045_art042.pdf) · Munipalli et al., HyPerComp/UCLA report (2008) [OSTI](https://www.osti.gov/servlets/purl/929194) · Eardley-Brunt, Dubas, Davis, PPCF 66:015015 (2024) [PDF](https://scientific-publications.ukaea.uk/wp-content/uploads/Eardley-Brunt_2024_Plasma_Phys._Control._Fusion_66_015015.pdf) · Blishchik, van der Lans, Kenjereš, IJHFF 90:108800 (2021) · Tassone, epotFoam report (2016) · Siriano et al. (2024) · Urgorri et al., PPCF 66:105007 (2024) · Planas, Badia, Codina, JCP 230:2977 (2011) · Votyakov, Kassinos, Albets-Chico [arXiv:0901.0624](https://arxiv.org/abs/0901.0624) · Pothérat, Sommeria, Moreau [arXiv:2006.06973](https://arxiv.org/abs/2006.06973) · Verzicco & Orlandi, JCP 123:402 (1996) · Oud et al., JCP 325:314 (2016) · Jiang & Smolentsev, FED 201:114262 (2024) · Wynne et al., Phys. Plasmas 32:013907 (2025) [arXiv:2409.08950](https://arxiv.org/abs/2409.08950) · Mistrangelo et al., Nucl. Fusion 65:116006 (2025) · Hyman & Shashkov, SIAM J. Numer. Anal. 36:788 (1999) · Celik et al., J. Fluids Eng. 130:078001 (2008).

JAX/GPU: jax-cfd source ([fast_diagonalization.py](https://raw.githubusercontent.com/google/jax-cfd/main/jax_cfd/base/fast_diagonalization.py), [pressure.py](https://raw.githubusercontent.com/google/jax-cfd/main/jax_cfd/base/pressure.py), [funcutils.py](https://raw.githubusercontent.com/google/jax-cfd/main/jax_cfd/base/funcutils.py)) · JAX-Fluids 2.0 [arXiv:2402.05193](https://arxiv.org/abs/2402.05193); 1.0 [arXiv:2203.13760](https://arxiv.org/abs/2203.13760) · jaxDecomp [JOSS 10.21105/joss.08852](https://joss.theoj.org/papers/10.21105/joss.08852.pdf) · JAX docs: [async dispatch](https://docs.jax.dev/en/latest/async_dispatch.html), [autodiff_remat](https://docs.jax.dev/en/latest/notebooks/autodiff_remat.html), [shard_map](https://docs.jax.dev/en/latest/notebooks/shard_map.html), [custom_linear_solve](https://docs.jax.dev/en/latest/_autosummary/jax.lax.custom_linear_solve.html), [profiling](https://docs.jax.dev/en/latest/profiling.html), [gradients and where](https://docs.jax.dev/en/latest/faq.html#gradients-contain-nan-where-using-where) · Blondel et al., NeurIPS 2022 [arXiv:2105.15183](https://arxiv.org/abs/2105.15183) · optimistix/lineax/diffrax adjoint docs · Exponax [`_navier_stokes.py`](https://raw.githubusercontent.com/Ceyron/exponax/main/exponax/stepper/_navier_stokes.py); APEBench [arXiv:2411.00180](https://arxiv.org/abs/2411.00180) · Kassam & Trefethen, SIAM J. Sci. Comput. 26:1214 (2005) · Blonigan, Gomez, Wang [arXiv:1401.4163](https://arxiv.org/abs/1401.4163) · NVIDIA RTX A4000 datasheet; GA102 whitepaper · SOLVAX README.

Q2D: Sommeria & Moreau, JFM 118:507 (1982) · Pothérat, Sommeria, Moreau, JFM 424:75 (2000) [arXiv:2006.15468](https://arxiv.org/abs/2006.15468) · Pothérat & Klein, JFM 761:168 (2014) · Baker et al., PRL 120:224502 (2018) · Sommeria, JFM 170:139 (1986) · Boffetta & Ecke, Annu. Rev. Fluid Mech. 44:427 (2012) · Cassells et al., JFM 861:382 (2019) · Camobreco, Pothérat, Sheard, PRF 6:013901 (2021); JFM Rapids (2023); PRF 10:023905 (2025) · Tithof et al., JFM 828:837 (2017) · Kochkov et al., PNAS 118 (2021) · Smolentsev & Moreau, CTR (2006) · Zikanov et al., Appl. Mech. Rev. 66:030802 (2014) · Brynjell-Rahkola, Duguet, Boeck, PRF 10:023903 (2025).

Thermal and devices: Lienhard & Lienhard, *A Heat Transfer Textbook*, 6th ed., §7.2 · Shah & London, *Laminar Flow Forced Convection in Ducts* (1978) · Gardner & Lykoudis, JFM 48:129 (1971) · Sukoriansky et al., FED 8:277 (1989) · Pirozzoli, JFM 966:A9 (2023) · ParaStell, Front. Nucl. Eng. 3 (2024) · VMEX, ESSOS repositories (uwplasma).

Data and comparators: FreeMHD [repo](https://github.com/PlasmaControl/FreeMHD), [Zenodo 13964055](https://zenodo.org/records/13964055) · `rogeriojorge/freemhd_install` · GridapMHD · UKAEA input files DOI 10.14468/43ys-0z67 · GeophysicalFlows.jl · Dedalus.

Process: pyOpenSci README guide · JOSS review criteria · Google small CLs · DESC workflows · GitHub self-hosted-runner security guidance · pytest-regressions, pytest-benchmark, asv · Nygard, ADRs · FAIR4RS · Diátaxis.

---

## 12. Instructions for an independent agent

1. **Before touching code**: `git fetch`; read `ROADMAP.md`, this Section 6 for
   your phase, and the ADRs; check open PRs and the nightly `benchmarks` branch;
   never work in another session's checkout (use a worktree).
2. **Pick the next unblocked step** in the dependency graph; open an issue with
   the step ID, the hypothesis, the budget and the exit criterion.
3. **Implement in one PR per step** (≤ 400 lines). Tests in the same PR. Run
   `python scripts/run_full_test_suite.py --changed-from origin/main` locally
   before pushing. Docs and the gallery entry ship with the step.
4. **Claim only what the gate measured.** The PR description quotes the numbers
   against the exit column and the validation rows; "implemented" alone is not
   a claim.
5. **Costly runs** (Docker, GPU, refinement) go to the nightly job or a labelled
   benchmark run, never to PR CI.
6. **When a step fails its gate**, write the numbers in the issue, decide
   (continue with a new bounded budget, change the operator via an ADR, or stop),
   and record the decision. Do not retune benchmarks to pass.
7. **Phase boundary**: update Section 2 with measured state, tick the exit gates,
   add the log row below, tag if Section 6 says so.
8. **Commits and PRs are authored by the project owner**, with no tool attribution.
9. **After the #122 privacy rewrite, use a fresh clone; do not merge
   pre-rewrite ancestry back.** Old commit identifiers in discussions are
   evidence, not checkout bases.
10. **Read Section 2 and the "Order of work" block first.** The next
    unblocked step is there; a step outside it needs the D21 exception stated
    in the issue.

---

## 13. Log

| Date | Work | Next |
|---|---|---|
| 2026-09-06 | Review of code, tests, PRs #1–#66, CI and FreeMHD Docker; five literature/software surveys; fresh runs (508/508 tests, seven examples, Hunt sweep to Ha 1000, Q2D 256², 3-D step-control bug, FreeMHD B2 smoke); #63 merged, #58–#62 closed; #64/#65/#66 reconciled into this plan; README and showcase assets | Phase 0.1: fix the VJP NaN; 0.2–0.6 in parallel |
| 2026-09-07 | Phase 0 closed: #69, #72, #73, #77, #79 merged; #80 was raised against #79's branch rather than `main`, so #84 landed the same commits on `main`. The pull-request regression tier had been killed by a 420-second budget with a 120-second per-test timeout while the slowest case needs 73 s locally; a tier and a file shard may now be combined and the tier runs as three disjoint shards at the 300-second timeout the other lanes use. A new check compares the shard entries against the files on disk in both directions, after an unsharded new test file made its module report zero combined coverage | Phase 1.3: face currents, Ohm's law and the face Lorentz force |
| 2026-09-07 | Phase 1.1 and 1.2 opened as #81, #82 and #83: `lmx.grid` (staggered grid, three stretching families, `wall_resolving_faces`), `lmx.bc` and `lmx.ops` (ghost-cell wall conditions; gradient and divergence exact discrete adjoints to a relative 1e-14 on a stretched mesh), and `lmx.poisson` (fast diagonalization; inverts the assembled operator to 1e-11 under four boundary combinations and matches a dense solve to 1e-12). Two accuracy properties are now pinned by test instead of implicit: the two-point wall flux is first order and keeps the operator symmetric, and the stretched interior gradient is centred between cell centres. Full local suite 614 passed in 124 s at 97.2% line coverage, with the four new modules at 100% line and branch | Merge the stack, then Phase 1.3 |
| 2026-09-07 | Phase 1 foundation merged. The four stacked steps were consolidated into #89 after each rebase onto a moving `main` cost the same architecture-budget conflict; #81, #82, #83 and #86 closed with commits preserved. #90 added the staggered Laplacian, #87 the fully developed inverse design, #88 the Q2D exact identities. Step 1.4 settled ADR 0002 by measurement: staggered nullspace 1 against collocated 8, weighted asymmetry 0 against 0.667 | Phase 1.5 |
| 2026-09-07 | Phase 1.5a merged as #91: the projection step. Plane-channel Poiseuille converges at second order (2.01, 2.04 over 8/16/32 cells; relative L2 5.3e-3 at 16). Two constraints had to be imposed rather than assumed, both found by testing: a wall-normal velocity is zero on its wall faces, and a periodic axis has its duplicated first and last face made equal, or the discrete divergence carries a boundary flux the pressure cannot remove. Magnetic damping is implicit, viscosity explicit with its limit reported; a cross-check against `solve_fully_developed_fields` was attempted and deferred to 1.5c because the two routes use different geometry and normalisation conventions | Phase 1.5b: implicit viscosity and advection |
| 2026-09-07 | Phase 1.5b and 1.6 landed as #92 (compiled `scan` trajectory with per-step checkpointing) and #93 (implicit viscosity through a separable Helmholtz factorization, which lifts the step off the mesh). #94 adds conservative limited momentum transport: the flux form conserves the momentum of a periodic box to round-off and converges at 1.91/1.97/1.99 on a smoothly stretched mesh against a hand-derived Taylor-Green flux divergence, with a van Leer blend that keeps a carried step inside its own bounds. `tests/test_core3d.py` became its own regression shard: the physics shard was running 420 s of its 420 s budget | Phase 1.5c |
| 2026-09-07 | Phase 1.5c found a real defect rather than a convention mismatch. An insulating wall carries no current, and the homogeneous Neumann Laplacian already drops its faces, but the motional term was still being differenced across them; the potential was then asked to absorb a boundary current the wall cannot carry. A square duct at Ha 20 ran at less than half its correct flow rate (0.0215 against 0.0499 for the maximum) and did not converge. `lmx.em.wall_insulated` closes the wall on both the source and the current. The conventions did reconcile once that was fixed: `GeometrySpec.width`/`height` are the full transverse extents, `CaseSpec.forcing` is `-dp/dx` entering divided by the density, and both routes read `viscosity` as the kinematic one, so a hydrodynamic duct agrees with `solve_fully_developed_fields` to a relative 2.6e-3 at 32 cells. A new spectral reference, `validation/shercliff.py` (Chebyshev collocation of the coupled system, converged to eight digits and sharing no operator with the package), gives the outside check: the new core converges to it at order 1.96 with a field on, and on a wall-resolving mesh reaches the Ha 20 flow rate to 1.3 % with 32 cells per direction | Phase 1.7: steady Newton-Krylov and the IFT adjoint |
| 2026-09-07 | Phase 1.7 landed as #96: `lmx.steady` solves the root of the projected steady momentum residual by matrix-free Newton-Krylov instead of marching a viscous time. The residual is written in the velocity alone, since the potential and the pressure follow from it by exact linear solves, and the damping rate is deliberately absent from it -- it is a preconditioning device, and the conservative face force already carries the whole Lorentz term; a first draft that subtracted it as well converged happily to the wrong steady state. Against `validation/shercliff.py` the duct is within 1.5 % at Ha 0 on 16 cells, 1.3 % at Ha 5 on 32, 1.3 % at Ha 20, 0.83 % at Ha 100, 1.0 % at Ha 300 and 0.5 % at Ha 1000 on wall-resolving meshes. The IFT adjoint matches central differences to 7e-12 in the drive and 1.2e-10 in the field scale; the tangent solve is wrapped in `jax.lax.custom_linear_solve` with an explicit transposed solve, because a Krylov basis is not a linear function of its right-hand side. Two findings about the linear solve are worth keeping. The limit was the **Krylov subspace, not the preconditioner**: at Ha 300, GMRES(60) with sixty restarts -- 3600 iterations -- reduces the residual by a factor of forty and stops, while GMRES(400) converges in 1077. And the obvious anisotropic preconditioner was **measured and rejected**: eliminating the potential leaves `-λ ∂_b ∇⁻² ∂_b`, whose symbol `ℓ_b/Σℓ` folds into the factorization for free, but it makes the iteration worse (residual 1.6 against 0.12 at Ha 300) because the potential obeys Neumann conditions and the velocity Dirichlet ones, so they do not share an eigenbasis in the Hartmann layer. The uniform rate is kept | Phase 1.7b: Hunt's thin conducting wall |
| 2026-09-07 | Phase 1.7b: Walker's thin-wall condition. A wall of conductance ratio `c = σ_w t_w/(σ a)` conducts along itself, and charge conservation in the sheet makes the wall-normal current `-c ∇²_τ φ`, evaluated on the layer of cells against the wall. It enters as one more face current, so the potential equation, the Lorentz force and the charge balance all see the same closure -- but the operator is no longer separable, so the exact factorization becomes the preconditioner of a Krylov solve wrapped in `jax.lax.custom_linear_solve`. Against the extended spectral reference a Hunt duct at Ha 20 is within 1.0 % at c 0.027 and 0.5 % at c 0.1 on 32 cells, and `c = 0` reproduces the insulating duct to 1e-14. Two things were fixed on the way: the stored face value points along the axis, so the wall current is the outward one on the upper wall and its negative on the lower, and the potential equation was solving `∇²φ = ∇·(σ u×B)` rather than `σ∇²φ = ∇·(σ u×B)`, which was wrong for any conductivity other than one -- a duct now gives the same answer at σ = 1 and σ = 4 with `Ha` held fixed. `electric_state` is now the one place that forms the face currents, shared by the step and the steady residual | Phase 1.8: the pipe O-grid |
| 2026-09-07 | Phase 1.9 first pass: `validation_ladder` in `scripts/make_showcase_figures.py` runs rows 2–4 through the staggered core and plots them against `validation/shercliff.py`. All nine cases (Ha 20/100/300 × c 0/0.027/0.1) land inside 2.3 % on a fixed 48² mesh, the Hartmann-layer profiles collapse onto `1 - e^-ξ` for every Ha with the spectral points on top of the lines, and the flow rate converges at second order. Two robustness fixes came out of building it: the symmetry guard in `lmx.poisson` measured asymmetry against the largest entry, which rejected a perfectly good wall-resolving mesh whose entries span eight orders of magnitude -- it is now relative to `sqrt(|d_i d_j|)`, the entry's own scale -- and the figure needs `enable_x64`, since a float32 solve stalls at a relative residual of 2e-5, three orders above the gate | Phase 1.8: the pipe O-grid |
| 2026-09-07 | Phase 1.10a: the staggered core is public. `lmx.duct_problem` builds a square insulating or Hunt duct from a Hartmann number, choosing both transverse meshes from the layers that exist rather than from a cell count, and `lmx.solve` dispatches a `ChannelProblem` to the steady solve, so a validated duct is three lines from `import lmx`. `wall_resolving_faces` gained `max_ratio=None`, which fits the gentlest stretching that spans the domain: the widths are rescaled to fill the half-width anyway, so a larger ratio buys no resolution, only an operator whose entries span orders of magnitude. The root API is four names larger at 33 | Phase 1.8: the pipe O-grid |
| 2026-09-07 | Phase 1.6 closed: `timeloop.energy_budget` measures the mechanical power crossing the duct -- drive, Lorentz work, viscous dissipation, fluid Joule dissipation and the electrical work delivered to a conducting wall. Two statements come out of it and they are kept apart. The mechanical defect is the steady residual projected onto the velocity and closes to 1e-13. The ohmic identity, that the Lorentz force does exactly minus the dissipation, is a property of the discretization: it is exact to 1e-12 on a uniform mesh with insulating walls, and first order through a conducting wall because the wall potential is taken as the adjacent cell value. Building it found a real defect in `ops.face_inner_product`: a periodic axis stores its wrap face twice and both copies carried full weight, so the measure of that control volume was doubled. An adjoint identity cannot see that -- both sides carry the same error -- but the energy budget did, as an exact factor of two. Restart is pinned by a chunked run agreeing with the whole to 1e-14 | Phase 1.8: the pipe O-grid |
| 2026-09-07 | Phase 1.8a: the polar metric. `Grid` carries a `geometry`, and the areas, volumes and azimuthal spacing follow from it, so the same flux-form operators solve a pipe. The axis needs no condition: the face at `r = 0` has zero area, which is the reason to write the divergence as a flux balance. Measured: a cylinder integrates exactly, the Laplacian of `1 - r²` is `-4` to 1e-12 everywhere except the wall cell, and a harmonic `r cos θ` converges at order 1.9–2.05 for `0.2 < r < 0.95`. Two honest limits are pinned rather than hidden. At the wall the two-point flux is first order and the cell volume is too, so the pointwise Laplacian carries an order-one error in that one cell -- the same closure Cartesian uses, and the reason the Poisson *solution* stays second order while this reading of the operator does not. At the axis an `m = 1` field loses an order, because the radial and azimuthal terms are each of order `1/r` and cancel. The separable stencils -- `staggered_laplacian`, the momentum transport and the fast-diagonalization assembly -- refuse a polar grid rather than answering on a metric they do not carry | Phase 1.8b: the polar Poisson, one eigendecomposition per azimuthal mode |
| 2026-09-07 | Phase 1.8b: the polar Poisson. The `1/r²` azimuthal term is what stops the polar Laplacian separating, and a discrete Fourier transform in the azimuth is what puts it back: mode `m` contributes `-4 sin²(πm/N)/(Δθ² r²)`, a *radial* diagonal, so each mode is separable in `(r, z)` with its own radial operator. One eigendecomposition per mode, `n_θ · n_r³` on the host and a batched contraction per solve. The radial operator is read out of the production stencil with the azimuth collapsed to one periodic cell, whose two faces carry the same value and so contribute nothing, and the axial and azimuthal metric factors cancel between the face area and the cell volume. It inverts its own operator to 2e-14 under both a Dirichlet and a Neumann radius, removes the mode-zero null space, and solves `∇²φ = -4` to `1 - r²` at order 2.00 -- the wall cell's order-one operator error does not reach the solution, as expected | Phase 1.8c: `pipe_problem` and a varying imposed field |
| 2026-09-07 | Phase 1.8c: `lmx.pipe`. A fully developed pipe has an axial velocity and, because `J x B` is then purely axial too, no cross flow to project -- so it is one scalar equation coupled to the potential, needing none of the staggered vector machinery. What the coordinates change is the electromotive force: a uniform Cartesian field is not uniform in polar components, `B_r = B cos θ` and `B_θ = -B sin θ`, so an axial velocity drives `B u sin θ` through a radial face and `B u cos θ` through an azimuthal one. The same face currents then carry both the charge balance and the axial force. The system is linear, so it is one preconditioned Krylov solve, preconditioned by the damped operator `(B² - ∇²)^-1` factorized exactly by the polar factorization. Measured: `Q/A = 1/8` at Ha 0 to order 2.00, clean second order in the azimuth at Ha 20 (differences 5.9e-5, 1.5e-5, 3.6e-6, 9.1e-7 as the count doubles), radial refinement converging to 0.035453 at Ha 20, charge conserved to 8e-11, and a conducting wall reducing the flow rate monotonically. `staggered_laplacian` now reads the metric through `Grid.axis_measures` -- written as a flux difference it is identical to the old second difference in Cartesian and is `(1/r) d/dr (r d/dr)` in polar, so the guard added in 1.8a is gone. What is missing is an outside reference at finite Ha, which is 1.8d | Phase 1.8d: a spectral pipe reference |
| 2026-09-07 | README rebuilt to show the physics rather than list it: a section per capability, each with its figure, the command that regenerates it, and three or four measured bullets -- ducts against the spectral reference, side layers to Ha 1000, pipes, gradient design, Q2D -- plus a table of what has been compared against other codes and what that comparison does and does not establish. The FreeMHD row states its own limit: an integration check on a harness mesh, not a production result, and ALEX B1/B2 acceptance still open | Keep each README claim tied to a measurement in the plan log |
| 2026-09-07 | Phase 2 unblocked: the office box is reachable, two RTX A4000s joined by PCIe with no NVLink. `scripts/run_benchmarks.py` keeps the compile time, the warm time and the time per step apart and writes JSON with an `accepted` flag judged against the precision the run used -- a fixed 1e-6 bound read a correct float32 run at 128³ as a failure, when its residual of 1.8e-6 is exactly machine precision there. Measured, 36 CPU cores against one A4000: the 3-D core at 128³ is 3.4× in float64 and 29.6× in float32; Q2D at 1024² is 17.0× and 54.5×. G4 is met in float32 and not in float64, and the reason is the card, not the code: an A4000 runs float64 at 1/64 of its float32 rate and the contractions are `O(N⁴)`, so float64 goes flop-bound (~1e10 FLOP/step against ~0.3 TFLOP/s ≈ 33 ms, measured 54 ms). The host-sync case measures directly what the timeloop tests assert structurally: the same trajectory at 10 and 40 steps costs the same per step. Two-GPU execution is blocked and the block is recorded rather than worked around | Phase 2.4a: let the Q2D constants replicate |
| 2026-09-07 | Phase 2.4a: the two-GPU block was `lmx.q2d` building its wavenumber grids with `jax.numpy` outside any trace, which commits them to one device so a sharded state cannot be combined with them. `solve_q2d` is never traced -- it reports a status, so it cannot be -- and handing the constants on as NumPy leaves them uncommitted. The differentiable path through `evolve_q2d` is untouched, because there they may depend on traced geometry. With that removed the 1-GPU and 2-GPU fields are **bit-identical** at 512², 1024² and 2048² in both precisions, and the strong-scaling efficiency of the unaided placement is 0.25–0.27 in float64 and 0.14–0.20 in float32: two devices cost about twice one, because the transforms all-gather every step across PCIe. The correctness half of G5 is met and the scaling half is not, with the reason measured rather than assumed | Phase 2.4b: a decomposition designed for the interconnect |
| 2026-09-07 | Phase 1.8d closes Phase 1: `validation/pipe.py` is a spectral pipe reference that shares no operator with the package. The axis is removed rather than treated -- the field is written on the *diameter* with an odd Chebyshev count so no point lands on the centre, and `f(-r, θ) = f(r, θ+π)` is built into the operators, so the axis is an interior point needing no condition. Against it the insulating pipe is within 0.04 %, 0.15 %, 0.37 % and 0.43 % at Ha 0, 5, 20 and 100, and the conducting wall converges at order 2.5–3.0, reaching 0.43 % at 96 radial cells: the thin-wall closure is first order in the wall potential and is the accuracy bottleneck, as the duct's energy budget also found. One trap: the area element carries `|r|`, whose kink at the axis costs a Chebyshev rule its accuracy, so the flow rate converged at *first* order while the field it integrates was exact to 1e-13. Gauss-Legendre on `[0, 1]` after a barycentric interpolation gives `1/8` exactly at every resolution | Phase 2 |
| 2026-09-13 | Review of `main` at `270063d`, #106–#123, the plan and its sources, with fresh measurements (Section 2) and five further literature and code surveys (Section 3.6). Findings: the float32 GPU numbers were TensorFloat-32 (accuracy 3e-4) and the "1/64" explanation of G4 was wrong (true float32 beats float64 by 0.9–12× depending on size); D4's mixed precision was never built and works through SOLVAX as-is (float64 accuracy at 2.4–4.5× the step speed on the A4000); the tangent and transpose solves are unpreconditioned and the adjoint fails from Ha 300 while the primal converges; the steady solver's GMRES(400) basis rules out 3-D and its iteration count grows linearly with Ha; the stretched-mesh electromagnetic interpolations are not discrete adjoints (asymmetry 3.7e-2, the first-order ohmic identity); the thin-wall closure is first order with an asymmetric operator (0.27) and its inner GMRES can be a direct solve (rank-2·n_y perturbation, Woodbury to 8e-12; Krasnov et al. 2023 fold it into the one-dimensional operators); the fully developed reporting loop cannot certify (#113) while the affine solve can; the new core is 2.2–13× slower than the legacy solve it should replace; Hua et al. 1988 is an inertialess SOR solution and ALEX walls are thick, so the B2 gates are re-tiered around the core-flow model; nothing since 2026-09-08 advanced 1.9, 1.10b or Phase 4. Decisions D15–D21 in ADR 0005; Sections 1, 2, 3.6, 6, 7, 9 and 12 revised; the order of work fixed | PR 2 of the order of work (2.0), then 1.7c and D19 |
| 2026-09-14 | Second batch: #135 (Stokes-limit PCG), #136 (second-order thin wall as a direct solve), #137 (field-line preconditioner: 714 → 68 CG iterations at Ha 1000), #144 (varying imposed field and the ANL fringe), and the main CI repairs #138–#140 and #143 (main had been red since before 270063d; the last cause was the steady coverage shard running a hosted runner out of memory). Two defects were found by gates rather than reports: a radial-only pipe refinement that stopped measuring the wall once the wall became second order, and a thin-wall current exerting J×B with no electromotive force. Parity (#141) met everywhere but Ha 1000 before #137. Opened #145 and #146. One agent PR description leaked local scratch paths and was scrubbed; the handoff now forbids machine paths in public text. Section 2.4 records the numbers | #145 and #146 in parallel; #141 re-measure; #142 GPU campaign; then 1.9b–1.9c |
| 2026-09-13 | First batch of the order of work merged within the day: #125 (Q2D real transforms), #126 (precision pin and provenance, stale claims), #127 (preconditioned tangent and exact transposed preconditioner), #128 (mixed-precision fast diagonalization), #129 (certified affine reporting, closes #113), #131 (adjoint-consistent electromagnetic interpolation). Two findings changed the picture: the #113 floor was a round-off mean subtraction in the potential solve rather than a stopping rule, and the adjoint defect already appeared at Ha 100. Agents ran focused tests locally and left the regression tier to hosted CI after five parallel local runs saturated the host; reviewed PRs with no failing check were admin-merged. Section 2.4 records the numbers | 1.7d part ii and 1.3b in progress; then 1.9a, 1.7e, 2.1 on an idle card |
| 2026-09-07 | Environment: the long-lived Python 3.11 venv caps at JAX 0.10.2, while a Python 3.13 venv installs JAX 0.11.1, the version the office box runs. The full suite passes on both, 642 tests in 138 s on 3.11 and 408 s on 3.13 with a cold compilation cache. Release gate 7.1 therefore has a local 3.13 result, though not a controlled speed comparison | Keep the 3.13 environment for the release smoke |
